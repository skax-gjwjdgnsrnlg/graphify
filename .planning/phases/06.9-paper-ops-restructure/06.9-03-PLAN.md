---
phase: 06.9-paper-ops-restructure
plan: 03
type: execute
wave: 3
depends_on: ["06.9-02"]
files_modified:
  - backend/src/main/java/com/graphify/trading/paper/dto/RunSummaryDto.java
  - backend/src/main/java/com/graphify/trading/paper/dto/RunDashboardDto.java
  - backend/src/main/java/com/graphify/trading/paper/PaperRunListController.java
  - backend/src/main/java/com/graphify/trading/paper/PaperRunDetailController.java
  - backend/src/main/java/com/graphify/trading/paper/PaperRunContributionService.java
  - backend/src/main/java/com/graphify/trading/paper/PaperHistoryService.java
  - backend/src/main/java/com/graphify/trading/paper/PaperReportService.java
  - backend/src/test/java/com/graphify/trading/paper/PaperRunListControllerTest.java
  - backend/src/test/java/com/graphify/trading/paper/PaperRunDashboardControllerTest.java
autonomous: true
requirements: [UIX-02]

must_haves:
  truths:
    - "GET /api/v1/trading/paper/runs가 실행중+종료 run을 RunSummaryDto 리스트로 반환한다 (전략명·상태·기간·유니버스·수익률·실현손익·거래수·최종자산·회차)"
    - "GET /runs/{runId}/dashboard가 run 기여분(실현·미실현·거래수·포지션) + account 전체 totalEquity/availableCash를 반환한다"
    - "GET /runs/{runId}/history가 run-scoped 또는 mode=RULE_AGGREGATE&from&to 통합 거래이력을 반환한다"
    - "GET /runs/{runId}/report가 run 기간 또는 RULE_AGGREGATE 기간의 ReportDto를 반환한다 (equity curve = 기간 내 계좌 전체 자산)"
  artifacts:
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperRunListController.java"
      provides: "GET /api/v1/trading/paper/runs"
      contains: "/api/v1/trading/paper/runs"
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperRunDetailController.java"
      provides: "GET /runs/{runId}/dashboard|history|report (mode 파라미터)"
      contains: "{runId}"
    - path: "backend/src/main/java/com/graphify/trading/paper/dto/RunSummaryDto.java"
      provides: "리스트 행 DTO"
      contains: "record RunSummaryDto"
  key_links:
    - from: "PaperRunListController"
      to: "PaperRunListService.listRuns"
      via: "HistoryService.requireCurrentUserId() → ApiResponse.ok"
      pattern: "listRuns"
    - from: "PaperRunDetailController dashboard"
      to: "PaperRunContributionService.dashboardContribution"
      via: "RunDashboardDto 매핑"
      pattern: "dashboardContribution"
---

<objective>
run-scoped REST API를 신설한다. 실행 이력 리스트(GET /runs)와 상세 3탭(dashboard/history/report) 엔드포인트를 만들고, history/report는 mode=RULE_AGGREGATE+기간 2-mode를 지원한다.

Purpose: Success Criteria #3(리스트 엔드포인트), #4(run-scoped + 2-mode 대시보드/이력/리포트). 프론트(Wave 4·5)가 소비할 계약.
Output: 2 DTO + 2 컨트롤러 + history/report 서비스의 run/기간 오버로드 + 통합 컨트롤러 테스트.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.9-paper-ops-restructure/06.9-RESEARCH.md
@.planning/phases/06.9-paper-ops-restructure/06.9-VALIDATION.md
@.planning/phases/06.9-paper-ops-restructure/06.9-02-SUMMARY.md

@backend/src/main/java/com/graphify/trading/paper/PaperDashboardController.java
@backend/src/main/java/com/graphify/trading/paper/PaperReportService.java
@backend/src/main/java/com/graphify/trading/paper/PaperHistoryService.java
@backend/src/main/java/com/graphify/trading/paper/dto/ReportDto.java
@backend/src/main/java/com/graphify/trading/paper/dto/PaperTradeHistoryItem.java
@backend/src/main/java/com/graphify/trading/paper/dto/PaperPositionItem.java

<interfaces>
<!-- 기존 컨트롤러 패턴 — 그대로 따른다. -->
컨트롤러 규약: `@RestController @RequestMapping("/api/v1/trading/paper/...")`, `Long userId = HistoryService.requireCurrentUserId();`, 반환 `ApiResponse.ok(...)`, `ApiResponse<T>` 래퍼.

재사용 DTO:
- ReportDto(equityCurve:List<EquityPoint{datetime,equity}>, totalReturn, maxDrawdownPct, winRate, totalTrades, winTrades, sharpeRatio, sortinoRatio, periodFrom:Instant, periodTo:Instant). ReportDto.empty().
- PaperTradeHistoryItem(id, tradedAt, symbol, companyName, side, qty, price, fee, pnl, rationaleJson). RESEARCH 권장: ruleId 필드 추가(선택). 추가 시 프론트 types/paper.ts(Wave 4)도 동기화.
- PaperPositionItem(symbol, companyName, qty, avgPrice, markPrice, marketValue, unrealizedPnl, unrealizedPnlPct).

Wave 2 서비스:
- PaperRunListService.listRuns(userId) → 내부 집계(전략명·상태·기간·유니버스·realizedPnl·returnPct·tradeCount·finalEquity·runIndex).
- PaperRunContributionService.dashboardContribution(userId, runId) / aggregate(ruleId, from, to).
- PaperRunRepository.findById, getRuleId/getStartedAt/getEndedAt/getStatus.

엔드포인트 응답 형태(RESEARCH "Endpoint Spec") — RunSummaryDto/RunDashboardDto JSON 참조.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunSummaryDto + RunDashboardDto + PaperRunListController(GET /runs) + 테스트</name>
  <files>backend/src/main/java/com/graphify/trading/paper/dto/RunSummaryDto.java, backend/src/main/java/com/graphify/trading/paper/dto/RunDashboardDto.java, backend/src/main/java/com/graphify/trading/paper/PaperRunListController.java, backend/src/test/java/com/graphify/trading/paper/PaperRunListControllerTest.java</files>
  <action>
RunSummaryDto record: (Long runId, Long ruleId, String ruleName, int runIndex, String status, Instant startedAt, Instant endedAt, List<String> universe, double realizedPnl, double returnPct, int tradeCount, double finalEquity). (RESEARCH JSON 형태.)
RunDashboardDto record: (Long runId, int runIndex, String ruleName, String status, double totalEquity, double availableCash, double realizedPnl, double unrealizedPnl, int tradeCount, List<PaperPositionItem> positions). PaperPositionItem 재사용.

PaperRunListController: `@RequestMapping("/api/v1/trading/paper/runs")`, `@GetMapping` getRuns() → userId=HistoryService.requireCurrentUserId(), `ApiResponse.ok(listService.listRuns(userId)→List<RunSummaryDto>)`. listService 내부 집계 record를 RunSummaryDto로 매핑(또는 listService가 직접 RunSummaryDto 반환하도록 Wave 2 record를 RunSummaryDto로 승격 — 둘 중 하나로 통일).

PaperRunListControllerTest 신규 (UIX-02-BE-5): listRuns 결과 컬럼(realizedPnl, returnPct, tradeCount, finalEquity, status RUNNING+STOPPED 혼재) 검증. 기존 컨트롤러/서비스 테스트 스타일(MockMvc 또는 서비스 mock) 중 PaperHistoryServiceTest 등 인접 테스트 패턴을 따른다.
  </action>
  <verify><automated>cd backend && ./gradlew test --tests "*.PaperRunListControllerTest"</automated></verify>
  <done>GET /runs가 RunSummaryDto 리스트 반환, 실행중+종료 모두 포함, 8+회차 컬럼 채워짐.</done>
</task>

<task type="auto">
  <name>Task 2: PaperRunDetailController (dashboard/history/report 3엔드포인트, 2-mode) + history/report 오버로드 + 테스트</name>
  <files>backend/src/main/java/com/graphify/trading/paper/PaperRunDetailController.java, backend/src/main/java/com/graphify/trading/paper/PaperRunContributionService.java, backend/src/main/java/com/graphify/trading/paper/PaperHistoryService.java, backend/src/main/java/com/graphify/trading/paper/PaperReportService.java, backend/src/test/java/com/graphify/trading/paper/PaperRunDashboardControllerTest.java</files>
  <action>
PaperHistoryService: run/기간 오버로드 추가 — getHistoryByRun(userId, runId) (findByRunIdOrderByTradedAtDesc 사용) + getHistoryByRuleAndPeriod(userId, ruleId, from, to). 기존 rationale JOIN 로직(rule_id+symbol+ts+signal) 재사용 — 기존 getHistory(userId) 본문을 trades 리스트 입력 받는 private 헬퍼로 추출 후 3 진입점이 공유.
PaperReportService: run/기간 오버로드 추가 — getReportByRun(userId, run.startedAt, run.endedAt|now) + getReportByPeriod(userId, from, to). 기존 getReport(userId) 30일 스냅샷 본문을 [from,to] 파라미터 받는 헬퍼로 추출(스냅샷 필터·MDD·Sharpe·Sortino·trade stats 로직 재사용). equity curve는 account 전체를 기간 필터 — "기간 내 계좌 전체 자산 흐름"(Open Q3, Pitfall 2). RULE_AGGREGATE realizedPnl은 rule_id+기간(PaperRunContributionService.aggregate) 기반.

PaperRunDetailController: `@RequestMapping("/api/v1/trading/paper/runs/{runId}")`.
- `@GetMapping("/dashboard")` → ApiResponse.ok(contributionService.dashboardContribution(userId, runId) → RunDashboardDto).
- `@GetMapping("/history")` 쿼리 `@RequestParam(required=false) String mode, from, to`: mode=RULE_AGGREGATE이면 run의 ruleId 조회 후 getHistoryByRuleAndPeriod, else getHistoryByRun(runId). → List<PaperTradeHistoryItem>.
- `@GetMapping("/report")` 동일 mode 분기 → ReportDto.
runId로 run 조회 실패 시 GraphifyException NOT_FOUND.

PaperRunDashboardControllerTest 신규 (UIX-02-BE-6): /dashboard가 run 기여분(realizedPnl/tradeCount/positions) + account 전체 totalEquity/availableCash 반환 검증.
  </action>
  <verify><automated>cd backend && ./gradlew test --tests "*.PaperRunDashboardControllerTest" --tests "*.PaperHistoryServiceTest"</automated></verify>
  <done>3 상세 엔드포인트 동작, history/report가 단일-run·RULE_AGGREGATE 2-mode 지원, 기존 getHistory/getReport 회귀 없음.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test` (full suite) green — Phase gate 전 필수.
- 컨트롤러 경로: /runs, /runs/{runId}/dashboard, /history, /report 4개 매핑 존재.
- 기존 PaperDashboard/History/Report/Monitor 컨트롤러·엔드포인트 삭제 0 (유지).
</verification>

<success_criteria>
Success Criteria #3·#4 충족: 실행중+종료 리스트 엔드포인트 + run-scoped/rule+기간 대시보드·이력·리포트 + 2-mode 기간 필터.
</success_criteria>

<output>
After completion, create `.planning/phases/06.9-paper-ops-restructure/06.9-03-SUMMARY.md`
</output>
