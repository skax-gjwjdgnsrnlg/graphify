---
phase: 06.9-paper-ops-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V37__paper_runs.sql
  - backend/src/main/java/com/graphify/trading/paper/PaperRun.java
  - backend/src/main/java/com/graphify/trading/paper/PaperRunRepository.java
  - backend/src/main/java/com/graphify/trading/paper/PaperTrade.java
  - backend/src/main/java/com/graphify/trading/paper/PaperLifecycleService.java
  - backend/src/test/java/com/graphify/trading/paper/PaperRunLifecycleServiceTest.java
  - backend/src/test/java/com/graphify/trading/paper/PaperLifecycleServiceOverrideTest.java
autonomous: true
requirements: [UIX-02]

must_haves:
  truths:
    - "룰 start() 1회가 paper_runs에 RUNNING 행 1건을 생성한다 (ruleId·userId·startedAt·universe_snapshot)"
    - "같은 전략을 재실행하면 새 run 행(회차)이 생성된다"
    - "룰 stop()이 해당 RUNNING run을 STOPPED로 종료하고 endedAt을 기록한다"
    - "paper_trades에 run_id 컬럼이 존재하고 nullable이다 (구형 데이터 NULL 허용)"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V37__paper_runs.sql"
      provides: "paper_runs 테이블 + paper_trades.run_id ALTER + 인덱스 + orphaned RUNNING 백필"
      contains: "CREATE TABLE IF NOT EXISTS paper_runs"
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperRun.java"
      provides: "PaperRun JPA 엔티티 (RUNNING/STOPPED, stop(endedAt))"
      contains: "class PaperRun"
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperRunRepository.java"
      provides: "findFirstByRuleIdAndStatus / findByRuleIdOrderByStartedAtDesc / findByUserIdOrderByStartedAtDesc"
      contains: "interface PaperRunRepository"
  key_links:
    - from: "PaperLifecycleService.start()"
      to: "paper_runs INSERT"
      via: "runRepo.save(new PaperRun(...)) within @Transactional"
      pattern: "runRepo\\.save"
    - from: "PaperLifecycleService.stop()"
      to: "run.stop(endedAt)"
      via: "findFirstByRuleIdAndStatus(ruleId, RUNNING).ifPresent"
      pattern: "findFirstByRuleIdAndStatus"
---

<objective>
백엔드 run(실행/세션) 도메인을 도입한다. V37 Flyway 마이그레이션으로 `paper_runs` 테이블을 만들고 `paper_trades`에 `run_id`를 추가한 뒤, `PaperLifecycleService.start()/stop()`이 run 행을 생성/종료하도록 연결한다.

Purpose: Success Criteria #1 — 룰 start→stop 1회 = 1 run(RUNNING/STOPPED, ruleId 연결, 재실행 시 새 run). 이후 모든 wave(태깅·집계·REST·프론트)의 데이터 토대.
Output: paper_runs 스키마 + PaperRun 엔티티/리포지토리 + start/stop run 라이프사이클 + 단위 테스트.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.9-paper-ops-restructure/06.9-RESEARCH.md
@.planning/phases/06.9-paper-ops-restructure/06.9-VALIDATION.md
@.planning/phases/06.9-paper-ops-restructure/06.9-CONTEXT.md

@backend/src/main/java/com/graphify/trading/paper/PaperLifecycleService.java
@backend/src/main/java/com/graphify/trading/paper/PaperTrade.java
@backend/src/test/java/com/graphify/trading/paper/PaperLifecycleServiceOverrideTest.java

<interfaces>
<!-- 직접 코드에서 추출. 그대로 사용 — 추가 탐색 불필요. -->

기존 PaperTrade 생성자 (PaperTrade.java:46) — 변경하지 말고 오버로드 추가:
```java
public PaperTrade(Long accountId, Long ruleId, String symbol, String side,
                  BigDecimal qty, BigDecimal price, BigDecimal pnl, Instant tradedAt)
```

기존 PaperLifecycleService 생성자 (line 54) — 6개 의존성, liveRanking에 @Qualifier:
```java
public PaperLifecycleService(TradingRuleRepository ruleRepo, ObjectMapper objectMapper,
    PaperLiveSymbolService paperLiveSymbolService, CompanyRepository companyRepo,
    @Qualifier("naverTradingValueRankingAdapter") VolumeRankingProvider liveRanking,
    MarketDataIngestionService ingestionService)
```
start() 본문(line 124): `rule.setRunStatus("RUNNING"); TradingRule saved = ruleRepo.save(rule); paperLiveSymbolService.assignSymbols(saved.getId(), symbols);` — symbols(List<String>)는 직전에 resolve됨.
stop() 본문(line 159): `rule.setRunStatus("STOPPED"); TradingRule saved = ruleRepo.save(rule); paperLiveSymbolService.deactivateRule(saved.getId());`

기존 테스트 생성자 호출 (PaperLifecycleServiceOverrideTest.java:64) — 6-arg, 7번째 의존성 추가 시 갱신 필요.
최신 마이그레이션: V36__instrument_type.sql → 다음 자유 번호 = V37.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V37 마이그레이션 + PaperRun 엔티티 + PaperRunRepository</name>
  <files>backend/src/main/resources/db/migration/V37__paper_runs.sql, backend/src/main/java/com/graphify/trading/paper/PaperRun.java, backend/src/main/java/com/graphify/trading/paper/PaperRunRepository.java</files>
  <action>
V37__paper_runs.sql 작성 (RESEARCH "V37 Migration" 블록 그대로):
- CREATE TABLE IF NOT EXISTS paper_runs: id BIGSERIAL PK, rule_id BIGINT NOT NULL, user_id BIGINT NOT NULL, started_at TIMESTAMPTZ NOT NULL DEFAULT now(), ended_at TIMESTAMPTZ (NULL=진행중), status VARCHAR(8) NOT NULL DEFAULT 'RUNNING', universe_snapshot TEXT, CHECK status IN ('RUNNING','STOPPED').
- 인덱스 3개: (rule_id), (user_id, started_at DESC), (status).
- ALTER TABLE paper_trades ADD COLUMN IF NOT EXISTS run_id BIGINT + idx_paper_trades_run.
- orphaned RUNNING 백필: INSERT INTO paper_runs(rule_id,user_id,started_at,status,universe_snapshot) SELECT tr.id, tr.user_id, tr.updated_at, 'RUNNING', NULL FROM trading_rules tr WHERE tr.run_status='RUNNING'.
paper_positions / paper_equity_snapshots는 절대 건드리지 않는다 (Pitfall 1, D5).

PaperRun.java: RESEARCH "JPA Entity: PaperRun" 블록 그대로 작성. @Entity @Table("paper_runs"), IDENTITY id, ruleId/userId/startedAt(Instant)/endedAt(Instant)/status/universeSnapshot. protected 기본생성자 + public PaperRun(Long ruleId, Long userId, Instant startedAt, String universeSnapshot) {status="RUNNING";...}. void stop(Instant endedAt){this.endedAt=endedAt; this.status="STOPPED";}. 모든 getter.

PaperRunRepository.java: extends JpaRepository<PaperRun, Long>. 메서드: findByUserIdOrderByStartedAtDesc(Long), findByRuleIdOrderByStartedAtDesc(Long), Optional<PaperRun> findFirstByRuleIdAndStatus(Long, String).
  </action>
  <verify><automated>cd backend && ./gradlew compileJava</automated></verify>
  <done>V37 SQL + PaperRun + PaperRunRepository 컴파일 통과. paper_positions/equity 미변경.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaperTrade.runId 오버로드 + start/stop run 라이프사이클 + 테스트</name>
  <files>backend/src/main/java/com/graphify/trading/paper/PaperTrade.java, backend/src/main/java/com/graphify/trading/paper/PaperLifecycleService.java, backend/src/test/java/com/graphify/trading/paper/PaperRunLifecycleServiceTest.java, backend/src/test/java/com/graphify/trading/paper/PaperLifecycleServiceOverrideTest.java</files>
  <behavior>
    - start() → runRepo.save 호출, 저장된 PaperRun.status=="RUNNING", ruleId/userId 일치, universe_snapshot = symbols JSON
    - 두 번째 start() (이전 stop 후) → 새 PaperRun.save 1회 추가 (회차 누적)
    - stop() → findFirstByRuleIdAndStatus(ruleId,"RUNNING") 결과의 stop(endedAt) 호출 + save, status=="STOPPED", endedAt != null
    - stop() 시 RUNNING run 없으면 예외 없이 무동작 (ifPresent)
  </behavior>
  <action>
PaperTrade.java: 기존 8-arg 생성자 유지(runId=null로 9-arg에 위임). `@Column(name="run_id") private Long runId;` 추가 + getRunId(). 신규 9-arg 생성자 `(accountId, ruleId, runId, symbol, side, qty, price, pnl, tradedAt)`. 8-arg는 `this(accountId, ruleId, null, symbol, side, qty, price, pnl, tradedAt)`로 위임 (06.5-03 PaperLedger 오버로드 위임 패턴 동일) — PaperExecutor 기존 호출은 Wave 2에서 9-arg로 교체하므로 지금은 깨지지 않음.

PaperLifecycleService.java: 생성자에 `PaperRunRepository runRepo` 7번째 파라미터 추가(필드+할당). @Qualifier 위치/다른 파라미터 순서 보존.
- start() 편집 (line 124 부근, rule.setRunStatus("RUNNING") 직후, assignSymbols 전후 무방하나 같은 @Transactional 내): `String universeJson = objectMapper.writeValueAsString(symbols);`(JsonProcessingException은 try/catch로 null 폴백) `runRepo.save(new PaperRun(saved.getId(), userId, Instant.now(), universeJson));` (Pitfall 5: run_status 가드와 동일 트랜잭션이므로 중복 방지).
- stop() 편집 (line 159 부근, setRunStatus("STOPPED") 직후): `runRepo.findFirstByRuleIdAndStatus(saved.getId(), "RUNNING").ifPresent(run -> { run.stop(Instant.now()); runRepo.save(run); });`
- import java.time.Instant 추가.

PaperRunLifecycleServiceTest.java 신규 (PaperLifecycleServiceOverrideTest 패턴 복제: @ExtendWith(MockitoExtension.class), @Mock 의존성들 + @Mock PaperRunRepository runRepo, ObjectMapper 실객체). start/재start/stop 3 시나리오를 ArgumentCaptor<PaperRun>로 검증. resolveSymbols가 liveRanking을 타지 않도록 overrideSymbols 경로 또는 symbols 정의를 활용.

PaperLifecycleServiceOverrideTest.java: 생성자 호출(line 64)에 `@Mock PaperRunRepository runRepo` 추가 + 7-arg로 갱신 (기존 테스트 깨짐 방지).
  </action>
  <verify><automated>cd backend && ./gradlew test --tests "*.PaperRunLifecycleServiceTest" --tests "*.PaperLifecycleServiceOverrideTest"</automated></verify>
  <done>PaperRunLifecycleServiceTest 3 시나리오 green. 기존 OverrideTest green. PaperTrade 8-arg 호출 하위호환 유지.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests "*.PaperRun*" --tests "*.PaperLifecycle*"` green.
- paper_positions / paper_equity_snapshots 스키마·엔티티 변경 0 (git diff 확인).
- V37이 최신 번호 (V36 다음).
</verification>

<success_criteria>
Success Criteria #1 충족: run 엔티티 존재 + start→stop 1회 = 1 run + 재실행 시 새 run + RUNNING/STOPPED + ruleId 연결. paper_trades.run_id nullable 컬럼 존재.
</success_criteria>

<output>
After completion, create `.planning/phases/06.9-paper-ops-restructure/06.9-01-SUMMARY.md`
</output>
