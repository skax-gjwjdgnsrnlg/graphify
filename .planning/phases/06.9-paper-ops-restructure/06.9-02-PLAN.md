---
phase: 06.9-paper-ops-restructure
plan: 02
type: execute
wave: 2
depends_on: ["06.9-01"]
files_modified:
  - backend/src/main/java/com/graphify/trading/paper/PaperExecutor.java
  - backend/src/main/java/com/graphify/trading/paper/PaperTradeRepository.java
  - backend/src/main/java/com/graphify/trading/paper/PaperRunListService.java
  - backend/src/main/java/com/graphify/trading/paper/PaperRunContributionService.java
  - backend/src/test/java/com/graphify/trading/paper/PaperExecutorRunTagTest.java
  - backend/src/test/java/com/graphify/trading/paper/PaperRunContributionServiceTest.java
  - backend/src/test/java/com/graphify/trading/paper/PaperExecutorTest.java
autonomous: true
requirements: [UIX-02]

must_haves:
  truths:
    - "BUY 체결 시 paper_trades.run_id = 해당 룰의 active(RUNNING) run id로 스탬핑된다"
    - "SELL 체결 시 paper_trades.run_id = active run id, pnl 함께 기록된다"
    - "active run이 없으면 run_id=null로 체결된다 (backward compat)"
    - "PaperRunListService가 run별 realizedPnl·returnPct·tradeCount·finalEquity·회차(runIndex)를 집계한다"
    - "PaperRunContributionService가 단일-run 기여분과 RULE_AGGREGATE(rule+기간) 통합 집계 2-mode를 지원한다"
  artifacts:
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperRunListService.java"
      provides: "listRuns(userId): paper_runs JOIN trading_rules + trade 집계 + runIndex"
      contains: "class PaperRunListService"
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperRunContributionService.java"
      provides: "run-scoped 대시보드 기여분 + run의 오픈포지션 파생 + 2-mode 기간 집계"
      contains: "class PaperRunContributionService"
  key_links:
    - from: "PaperExecutor.executeBuy/executeSell"
      to: "paper_trades.run_id"
      via: "resolveActiveRunId(rule.getId()) → 9-arg PaperTrade 생성자"
      pattern: "resolveActiveRunId"
    - from: "PaperRunContributionService run 오픈포지션"
      to: "paper_positions"
      via: "run_id BUY without SELL symbol set → positionRepo 조회 (positions에 run_id 추가 안 함)"
      pattern: "findByRunId"
---

<objective>
체결 시 run_id 스탬핑(단일 편집 지점 2곳)과 집계 서비스 2종을 추가한다. `PaperRunListService`는 실행 이력 리스트 컬럼을, `PaperRunContributionService`는 상세 대시보드 기여분과 기간 필터 2-mode 집계를 계산한다.

Purpose: Success Criteria #2(태깅·기여분 집계), #3(리스트 컬럼), #4(2-mode 집계)의 서비스 계층.
Output: run_id 스탬핑된 PaperExecutor + 2개 집계 서비스 + 단위 테스트.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.9-paper-ops-restructure/06.9-RESEARCH.md
@.planning/phases/06.9-paper-ops-restructure/06.9-VALIDATION.md
@.planning/phases/06.9-paper-ops-restructure/06.9-01-SUMMARY.md

@backend/src/main/java/com/graphify/trading/paper/PaperExecutor.java
@backend/src/main/java/com/graphify/trading/paper/PaperDashboardService.java
@backend/src/test/java/com/graphify/trading/paper/PaperExecutorTest.java

<interfaces>
<!-- Wave 1 산출물 + 기존 코드. -->
PaperRunRepository.findFirstByRuleIdAndStatus(Long ruleId, String status): Optional<PaperRun> (Wave 1).
PaperRun: getId(), getRuleId(), getStartedAt():Instant, getEndedAt():Instant, getStatus(), getUniverseSnapshot().
PaperTrade 9-arg 생성자 (Wave 1): (accountId, ruleId, runId, symbol, side, qty, price, pnl, tradedAt).

PaperExecutor 편집 지점 (현재 라인):
- executeBuy() line 108: `tradeRepo.save(new PaperTrade(account.getId(), rule.getId(), symbol, "BUY", bdQty, bdPrice, null, ts));`
- executeSell() line 136: `tradeRepo.save(new PaperTrade(account.getId(), rule.getId(), symbol, "SELL", bdQty, bdPrice, bdPnl, ts));`
PaperExecutor 생성자(line 39): 6 의존성 (accountRepo, positionRepo, tradeRepo, signalLogRepo, fillSimulator, marketBarRepository).
PaperExecutorTest 생성자 호출(line ~41): 6-arg.

PaperTradeRepository 기존: findByAccountIdOrderByTradedAtDesc(Long), findByRuleIdOrderByTradedAtDesc(Long).
PaperEquitySnapshotRepository: findByAccountIdOrderByTsDesc(Long accountId).
PaperPositionRepository: findByAccountId(Long), findByAccountIdAndSymbol(Long,String).
PaperDashboardService.toPositionItem 패턴: markPrice = intradayRepo.findBySymbolAndIntervalOrderByTsAsc(symbol,"5m") 마지막 close; SymbolNameService.resolve(symbol).

집계 규칙 (RESEARCH "Contribution Aggregation Spec"):
- realizedPnl = SUM(pnl) WHERE run_id=:runId AND side='SELL'
- returnPct = realizedPnl / SUM(qty*price WHERE run_id=:runId AND side='BUY') * 100 (분모=BUY 실투자원금; Open Q2 권장)
- tradeCount = COUNT(WHERE run_id=:runId AND side='BUY') (진입 횟수)
- finalEquity: RUNNING=현재 account equity, STOPPED=ended_at 이전 마지막 스냅샷 equity
- runIndex(#회차) = 동일 rule_id의 started_at ASC 순서 번호 (실시간 계산; Open Q1 권장 — DB 저장 안 함)
- run 오픈포지션 = run_id BUY symbol 중 같은 run_id SELL 없는 symbol → paper_positions 조회 (positions에 run_id 추가 금지, Pitfall 1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaperExecutor run_id 스탬핑 (편집 지점 2곳) + 테스트</name>
  <files>backend/src/main/java/com/graphify/trading/paper/PaperExecutor.java, backend/src/test/java/com/graphify/trading/paper/PaperExecutorRunTagTest.java, backend/src/test/java/com/graphify/trading/paper/PaperExecutorTest.java</files>
  <behavior>
    - active RUNNING run 존재 → BUY 저장된 PaperTrade.runId == run.id
    - active RUNNING run 존재 → SELL 저장된 PaperTrade.runId == run.id, pnl 기록
    - active run 없음(Optional.empty) → runId == null (체결은 정상 진행)
  </behavior>
  <action>
PaperExecutor.java:
- 생성자에 `PaperRunRepository runRepo` 7번째 파라미터 추가 (필드 + 할당).
- private 헬퍼: `private Long resolveActiveRunId(Long ruleId){ return runRepo.findFirstByRuleIdAndStatus(ruleId,"RUNNING").map(PaperRun::getId).orElse(null); }`
- executeBuy() 편집 지점: `Long runId = resolveActiveRunId(rule.getId());` 후 9-arg 생성자 `new PaperTrade(account.getId(), rule.getId(), runId, symbol, "BUY", bdQty, bdPrice, null, ts)`.
- executeSell() 편집 지점: 동일하게 runId resolve 후 9-arg 생성자에 bdPnl 전달.
Pitfall 3 주석 남김 (틱당 룰별 1회 lookup, 성능 이슈 시 Phase 후속에서 OrderExecutorPort에 runId 파라미터화).

PaperExecutorRunTagTest.java 신규: PaperExecutorTest 패턴 복제 + @Mock PaperRunRepository runRepo. BUY/SELL/no-run 3 케이스를 ArgumentCaptor<PaperTrade>로 runId 검증. positionRepo·accountRepo stub은 기존 테스트 참조.

PaperExecutorTest.java: 생성자 호출에 mock runRepo 추가(7-arg)로 갱신.
  </action>
  <verify><automated>cd backend && ./gradlew test --tests "*.PaperExecutorRunTagTest" --tests "*.PaperExecutorTest"</automated></verify>
  <done>BUY/SELL run_id 스탬핑 + no-run null 케이스 green. 기존 PaperExecutorTest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaperRunListService + PaperRunContributionService + 리포지토리 쿼리 + 테스트</name>
  <files>backend/src/main/java/com/graphify/trading/paper/PaperTradeRepository.java, backend/src/main/java/com/graphify/trading/paper/PaperRunListService.java, backend/src/main/java/com/graphify/trading/paper/PaperRunContributionService.java, backend/src/test/java/com/graphify/trading/paper/PaperRunContributionServiceTest.java</files>
  <behavior>
    - RULE_AGGREGATE 모드: rule_id + [from,to] 필터로 realizedPnl 합산 (run_id=NULL 구형 거래 포함)
    - 단일-run 모드: run_id 스코프 기여분 (realizedPnl, tradeCount, 오픈포지션 파생)
    - run 오픈포지션 = run의 BUY symbol − 같은 run SELL symbol → positions 조회
  </behavior>
  <action>
PaperTradeRepository.java 메서드 추가:
- `List<PaperTrade> findByRunIdOrderByTradedAtDesc(Long runId)` (단일-run; NULL 자동 제외, Pitfall 6).
- `List<PaperTrade> findByRuleIdAndTradedAtBetweenOrderByTradedAtDesc(Long ruleId, Instant from, Instant to)` (RULE_AGGREGATE; 구형 NULL run_id 포함).

PaperRunListService.java (@Service @Transactional(readOnly=true)):
- 의존성: PaperRunRepository, TradingRuleRepository, PaperTradeRepository, PaperAccountRepository, PaperEquitySnapshotRepository.
- listRuns(Long userId): runRepo.findByUserIdOrderByStartedAtDesc(userId). 각 run에 대해 ruleName=ruleRepo 조회, trades=findByRunIdOrderByTradedAtDesc 인메모리 집계(realizedPnl=SELL pnl 합, denominator=BUY qty*price 합, returnPct, tradeCount=BUY 수), universe=universeSnapshot JSON 파싱(List<String>), finalEquity(RUNNING=account equity / STOPPED=ended_at 이전 마지막 스냅샷). runIndex는 동일 ruleId 그룹의 startedAt ASC 인덱스로 실시간 부여. 반환은 Wave 3에서 정의할 RunSummaryDto 대신 이 plan에서는 내부 집계 record(예: RunListItem) 또는 Map 형태로 반환하고 DTO 매핑은 Wave 3 컨트롤러에서 수행 — 단, 컬럼 계산 로직은 전부 이 서비스에 둔다.

PaperRunContributionService.java (@Service @Transactional(readOnly=true)):
- 의존성: PaperRunRepository, PaperTradeRepository, PaperAccountRepository, PaperPositionRepository, PaperEquitySnapshotRepository, MarketBarIntradayRepository, SymbolNameService (대시보드 기여분; PaperDashboardService 마크프라이스/네임 패턴 재사용).
- dashboardContribution(userId, runId): totalEquity/availableCash=account 전체, realizedPnl/tradeCount=run 스코프, unrealizedPnl + positions=run 오픈포지션 파생(BUY symbol − SELL symbol → positionRepo 조회 → markPrice 평가). positions에 run_id 추가하지 않는다.
- aggregate(ruleId, from, to): RULE_AGGREGATE 모드 — findByRuleIdAndTradedAtBetween으로 realizedPnl/거래수 집계 (구형 NULL run_id 포함).
반환 타입은 내부 record 또는 기존 PaperPositionItem 재사용; Wave 3에서 RunDashboardDto로 매핑.

PaperRunContributionServiceTest.java 신규 (@ExtendWith(MockitoExtension.class)): RULE_AGGREGATE 기간 집계(rule_id 스코프, NULL run_id 포함)와 단일-run 기여분(오픈포지션 파생) 검증. UIX-02-BE-7.
  </action>
  <verify><automated>cd backend && ./gradlew test --tests "*.PaperRunContributionServiceTest"</automated></verify>
  <done>2-mode 집계·오픈포지션 파생 green. 리스트 집계 컬럼 로직 완비. paper_positions 미변경.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test` (full suite) green (Wave merge 게이트).
- run_id 스탬핑이 executeBuy/executeSell 양쪽에 적용 (grep resolveActiveRunId = 2 호출).
- paper_positions 엔티티/스키마 변경 0.
</verification>

<success_criteria>
Success Criteria #2 충족(거래 run/ruleId 태깅 + 단일 계좌 기여분 집계), #3·#4의 집계 로직 완성(리스트 컬럼 + 2-mode 기간 필터).
</success_criteria>

<output>
After completion, create `.planning/phases/06.9-paper-ops-restructure/06.9-02-SUMMARY.md`
</output>
