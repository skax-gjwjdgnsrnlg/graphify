---
phase: 06.9-paper-ops-restructure
plan: 04
type: execute
wave: 4
depends_on: ["06.9-03"]
files_modified:
  - frontend/src/layouts/TradingLayout.tsx
  - frontend/src/router/index.tsx
  - frontend/src/pages/trading/TradingRulesPage.tsx
  - frontend/src/lib/paperApi.ts
  - frontend/src/types/paper.ts
  - frontend/src/pages/trading/TradingMonitorPage.tsx
  - frontend/src/pages/trading/paper/PaperDashboardPage.tsx
  - frontend/src/pages/trading/paper/PaperHistoryPage.tsx
  - frontend/src/pages/trading/paper/PaperReportPage.tsx
autonomous: false
requirements: [UIX-02]

must_haves:
  truths:
    - "PAPER nav가 3그룹(전략 / 운영 결과 / 공통)으로 재편되고 '운영 결과 > 실행 이력'(/trading/paper/runs) 엔트리가 보인다"
    - "동작 모니터링 nav·라우트·페이지가 제거된다 (PaperMonitorController 백엔드는 유지)"
    - "토스 설정 nav는 PAPER 모드에서 숨고 LIVE 모드에서만 노출된다 (/trading/settings 라우트는 유지)"
    - "전략 운영 화면(TradingRulesPage) 상단에 시장 상태 + 스케줄러 최근 실행 카드가 표시된다 (신호로그·오늘 거래 미노출)"
    - "신규 run API(fetchPaperRuns/fetchRunDashboard/fetchRunHistory/fetchRunReport)와 타입이 추가된다"
  artifacts:
    - path: "frontend/src/lib/paperApi.ts"
      provides: "run-scoped fetch 함수 5종"
      contains: "paper/runs"
    - path: "frontend/src/types/paper.ts"
      provides: "RunSummary / RunDashboard 타입"
      contains: "RunSummary"
    - path: "frontend/src/layouts/TradingLayout.tsx"
      provides: "3그룹 nav + 토스 조건부 + 실행 이력 엔트리"
      contains: "운영 결과"
  key_links:
    - from: "TradingLayout commonItems"
      to: "토스 설정 조건부"
      via: "mode === 'LIVE' 필터"
      pattern: "mode === .LIVE."
    - from: "TradingRulesPage 상단 위젯"
      to: "GET /paper/monitor"
      via: "fetchPaperMonitor() marketStatus + schedulerLastRun"
      pattern: "fetchPaperMonitor"

ui_ux: true
---

<objective>
프론트 정보구조를 재편한다. nav 3그룹화 + 토스 PAPER 숨김 + 동작 모니터링/대시보드/이력/리포트 페이지·라우트 제거, 전략 운영 상단 시장·스케줄러 위젯 추가, 그리고 Wave 5가 소비할 run API/타입을 추가한다.

Purpose: Success Criteria #5(통합 메뉴 대체 기반), #6(모니터링 제거 + 위젯 이동), #7(토스 PAPER 숨김).
Output: 재편된 nav/router + 위젯 + paperApi/types run 함수 + 폐기 페이지 4종 삭제.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.9-paper-ops-restructure/06.9-RESEARCH.md
@.planning/phases/06.9-paper-ops-restructure/06.9-03-SUMMARY.md
@wireframe-6.9.html

@frontend/src/layouts/TradingLayout.tsx
@frontend/src/router/index.tsx
@frontend/src/lib/paperApi.ts
@frontend/src/types/paper.ts
@frontend/src/pages/trading/TradingRulesPage.tsx

<interfaces>
<!-- 기존 패턴 — 그대로. -->
mode: `const mode = useTradingStore((s) => s.mode);` ("PAPER"|"LIVE").
nav 항목: `interface NavItem { to:string; label:string; end?:boolean }`. paperItems / liveItems / commonItems 배열. SidebarNav가 modeItems + commonItems 렌더.
API: `apiGet<T>(path)`, `apiPost<Req,Res>(path, body)` from "@/lib/apiClient". fetchPaperMonitor() → MonitorData{ schedulerLastRun:string|null, marketStatus:"OPEN"|"CLOSED", recentSignals, todayTrades }.
UI 프리미티브: TradeCard, TradeStatCard, TradeBadge(variant up/down/draft/running), TradePageState(variant loading/error/empty) — from "@/components/trading/ui".
Wave 3 백엔드 계약(RESEARCH Endpoint Spec): GET /runs → RunSummaryDto[]; /runs/{id}/dashboard → RunDashboardDto; /runs/{id}/history?mode&from&to → PaperTradeHistoryItem[]; /runs/{id}/report?mode&from&to → ReportDto.
토큰 규칙: trade-* 토큰만, 하드코딩 hex 금지 (CLAUDE.md). 다크 페이지엔 shared/ EmptyState 대신 TradePageState(06.6 결정).
</interfaces>
</context>

## UI/UX

화면 인벤토리 (이 plan 범위):
- TradingLayout 사이드바 nav: 3그룹 헤더("전략" / "운영 결과" / "공통") + 항목. 상태 없음(정적). 활성 링크 = bg-trade-elevated border-l-2 border-trade-primary.
- 전략 운영(TradingRulesPage) 상단 위젯 2카드: 시장 상태(open=dot trade-up / closed=trade-muted), 스케줄러 최근 실행(시각/없음). 상태: loading→TradePageState loading 또는 카드 스켈레톤, error→조용히 카드 숨김 또는 "—", success→값.

shared-reuse 매핑:
- 시장 상태/스케줄러 카드: TradeCard + TradeStatCard 재사용 (신규 컴포넌트 없음).
- nav: 기존 TradingLayout 구조 확장 (신규 컴포넌트 없음).

반응형/포커스: 기존 lg 사이드바 + 모바일 드로어 패턴 유지. nav 링크는 NavLink(키보드 탭 가능). 위젯 카드 grid-cols-2(데스크탑) → 1열(모바일).

<tasks>

<task type="auto">
  <name>Task 1: nav 3그룹 재편 + 토스 조건부 숨김 + 폐기 라우트/페이지 제거 + run API/타입</name>
  <files>frontend/src/layouts/TradingLayout.tsx, frontend/src/router/index.tsx, frontend/src/lib/paperApi.ts, frontend/src/types/paper.ts, frontend/src/pages/trading/TradingMonitorPage.tsx, frontend/src/pages/trading/paper/PaperDashboardPage.tsx, frontend/src/pages/trading/paper/PaperHistoryPage.tsx, frontend/src/pages/trading/paper/PaperReportPage.tsx</files>
  <action>
TradingLayout.tsx (RESEARCH "Nav 변경"):
- paperItems 재구성 → 그룹화. SidebarNav를 단일 modeGroupLabel 1그룹에서 PAPER 모드일 때 3그룹 렌더로 변경: [전략] 전략 설정(/trading/paper/rules)·전략 운영(/trading/paper/rules-lifecycle)·백테스트(/trading/paper/backtest); [운영 결과] 실행 이력(/trading/paper/runs); [공통] DDS Agent(/trading, end)·(토스 설정은 조건부). LIVE 모드는 기존 liveItems 유지.
- 모의 대시보드·동작 모니터링·모의 거래 이력·모의 성과 리포트 엔트리 제거.
- commonItems의 토스 설정: `mode === 'LIVE'`일 때만 렌더 (PAPER 숨김, D4/Pitfall 4 — 라우트는 유지).
- 그룹 헤더 스타일은 기존 modeGroupLabel `<p>` 클래스 재사용.

router/index.tsx:
- 제거: `monitor`(TradingMonitorPage), `paper/dashboard`(PaperDashboardPage), `paper/history`(PaperHistoryPage), `paper/report`(PaperReportPage) 라우트 + 해당 import 4개.
- `settings`(TossSettingsPage) 라우트는 유지 (D4).
- /trading/paper/runs 라우트는 Wave 5(Plan 05)에서 추가 — 이 plan에서는 추가하지 않음.

폐기 페이지 파일 삭제: TradingMonitorPage.tsx, PaperDashboardPage.tsx, PaperHistoryPage.tsx, PaperReportPage.tsx (RESEARCH "삭제할 페이지 파일"). PaperMonitorController(백엔드)·/paper/monitor 엔드포인트는 건드리지 않는다.

types/paper.ts 추가:
- `RunSummary { runId; ruleId; ruleName; runIndex; status:"RUNNING"|"STOPPED"; startedAt; endedAt:string|null; universe:string[]; realizedPnl; returnPct; tradeCount; finalEquity }`.
- `RunDashboard { runId; runIndex; ruleName; status; totalEquity; availableCash; realizedPnl; unrealizedPnl; tradeCount; positions: PaperPositionItem[] }`.
- PaperTradeHistoryItem에 ruleId 추가 시 동기화(백엔드 선택사항과 일치).

paperApi.ts 추가:
- `fetchPaperRuns()` → apiGet<RunSummary[]>(`${BASE}/runs`).
- `fetchRunDashboard(runId)` → apiGet<RunDashboard>(`${BASE}/runs/${runId}/dashboard`).
- `fetchRunHistory(runId, mode?, from?, to?)` → apiGet<PaperTradeHistoryItem[]> 쿼리스트링 조립.
- `fetchRunReport(runId, mode?, from?, to?)` → apiGet<ReportData>(... /report).
  </action>
  <verify><automated>cd frontend && npm run build</automated></verify>
  <done>빌드 green. nav 3그룹·토스 조건부·모니터링 제거 반영. 폐기 라우트 import 잔존 0. run API/타입 export됨.</done>
</task>

<task type="auto">
  <name>Task 2: 전략 운영 상단 시장·스케줄러 위젯</name>
  <files>frontend/src/pages/trading/TradingRulesPage.tsx</files>
  <action>
TradingRulesPage.tsx 상단(h1 아래, 전략 테이블 위)에 시장 상태·스케줄러 위젯 추가 (RESEARCH "TradingRulesPage 수정"):
- fetchPaperMonitor() 호출(기존 useQuery/useEffect 패턴 따름; 페이지의 기존 데이터 패칭 방식 확인 후 일치).
- `<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">` 안에 2카드:
  - 시장 상태: TradeCard + 점(open=trade-up dot, closed=trade-muted) + "개장"/"폐장" 텍스트(marketStatus OPEN/CLOSED 매핑).
  - 스케줄러 최근 실행: TradeStatCard(label "스케줄러 최근 실행", value=schedulerLastRun 포맷 또는 "—").
- 신호로그·오늘 거래는 렌더하지 않는다 (D3).
- 토큰만 사용(하드코딩 hex 금지). 로딩 시 카드 스켈레톤 또는 "—", 에러 시 카드 숨김/"—"로 graceful.
  </action>
  <verify><automated>cd frontend && npm run build && grep -rEc '#[0-9a-fA-F]{3,6}' frontend/src/pages/trading/TradingRulesPage.tsx</automated></verify>
  <done>빌드 green, TradingRulesPage 하드코딩 hex 0건, 상단 2카드(시장·스케줄러) 렌더.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: IA·nav·위젯 시각 검증</name>
  <what-built>nav 3그룹 재편(전략/운영 결과/공통), 토스 PAPER 숨김·LIVE 노출, 동작 모니터링 제거, 전략 운영 상단 시장·스케줄러 위젯.</what-built>
  <how-to-verify>
1. `cd frontend && npm run dev` 후 /trading 진입 (PAPER 모드).
2. 사이드바: [전략](전략 설정·전략 운영·백테스트) / [운영 결과](실행 이력) / [공통](DDS Agent) 3그룹 확인. "동작 모니터링"·"모의 대시보드"·"모의 거래 이력"·"모의 성과 리포트"·"토스 설정" 미표시 확인.
3. 모드 토글 → LIVE: 공통에 "토스 설정" 표시 확인.
4. /trading/paper/rules-lifecycle(전략 운영): 상단에 시장 상태 + 스케줄러 최근 실행 카드 2개 확인, 신호로그·오늘 거래 미표시.
5. /trading/monitor 직접 접근 → 라우트 없음(404/리다이렉트). /trading/settings 직접 접근 → 여전히 동작(라우트 유지).
  </how-to-verify>
  <action>위 how-to-verify 단계를 실행해 nav 3그룹·토스 조건부·모니터링 제거·전략 운영 상단 위젯을 육안 확인한다. 자동화 빌드는 Task 1·2에서 완료됨 — 이 단계는 시각 검증 게이트.</action>
  <verify>사용자가 PAPER/LIVE 모드 전환하며 nav·위젯·라우트 제거를 육안 확인</verify>
  <done>사용자가 "approved" 응답 (또는 문제점 수정 후 재확인)</done>
  <resume-signal>"approved" 또는 문제점 기술</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` green.
- 삭제된 4개 페이지 파일 부재, router import 잔존 0.
- PaperMonitorController(백엔드) 및 /paper/monitor 엔드포인트 미변경.
- /trading/settings 라우트 유지.
</verification>

<success_criteria>
Success Criteria #6(모니터링 제거 + 위젯 이동), #7(토스 PAPER 숨김/LIVE 노출), #5 일부(통합 메뉴 진입점 nav). run API/타입 준비 완료(Plan 05 소비).
</success_criteria>

<output>
After completion, create `.planning/phases/06.9-paper-ops-restructure/06.9-04-SUMMARY.md`
</output>
