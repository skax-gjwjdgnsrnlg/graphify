---
phase: 00-data-infra-dynamic-universe
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/graphify/trading/backtest/BacktestService.java
  - backend/src/test/java/com/graphify/trading/backtest/BacktestServiceVolumeTest.java
  - backend/src/test/java/com/graphify/trading/engine/RuleEvaluatorVolumeTest.java
autonomous: true
requirements:
  - DATA-05

must_haves:
  truths:
    - "VOLUME > 30000 진입 룰을 가진 백테스트에서 volume=50000인 봉에서 진입 신호가 발생한다"
    - "BacktestService가 closes[]와 동일한 방식으로 volumes[]를 추출해 RuleEvaluator에 전달한다"
    - "RuleEvaluator.operand()가 volumes != null && volumes[i] != null일 때 실제 값을 반환한다"
  artifacts:
    - path: "backend/src/main/java/com/graphify/trading/backtest/BacktestService.java"
      provides: "volumes 배열 추출 및 RuleEvaluator 전달"
      contains: "volumesBySymbol"
    - path: "backend/src/test/java/com/graphify/trading/backtest/BacktestServiceVolumeTest.java"
      provides: "DATA-05 버그 수정 회귀 테스트"
      exports: ["BacktestServiceVolumeTest"]
    - path: "backend/src/test/java/com/graphify/trading/engine/RuleEvaluatorVolumeTest.java"
      provides: "VOLUME 지표 조건 단위 테스트"
      exports: ["RuleEvaluatorVolumeTest"]
  key_links:
    - from: "BacktestService.run()"
      to: "RuleEvaluator.entryTriggered()"
      via: "volumesBySymbol.get(symbol)"
      pattern: "volumesBySymbol\\.get"
    - from: "RuleEvaluator.operand()"
      to: "volumes[i]"
      via: "case VOLUME"
      pattern: "volumes != null && volumes\\[i\\] != null"
---

<objective>
BacktestService의 volume null 전달 버그를 수정한다.

Purpose: VOLUME 지표 기반 룰이 현재 volumes=null 전달로 인해 항상 false를 반환한다. Bar.volume()은 이미 Double 타입으로 존재하고 DbMarketDataAdapter도 이미 정상 변환 중이나, BacktestService가 closes[]만 추출하고 volumes[]를 추출하지 않아 발생한다.
Output: BacktestServiceVolumeTest.java, RuleEvaluatorVolumeTest.java (Wave 0 테스트 스텁), 수정된 BacktestService.java
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-data-infra-dynamic-universe/00-RESEARCH.md
@.planning/phases/00-data-infra-dynamic-universe/00-VALIDATION.md

<interfaces>
<!-- 수정 대상 코드의 핵심 타입. 탐색 불필요 — 아래 계약을 그대로 사용하라. -->

From backend/src/main/java/com/graphify/trading/engine/Bar.java:
```java
public record Bar(LocalDate date, double close, Double volume) {
    public static Bar ofClose(LocalDate date, double close) { return new Bar(date, close, null); }
}
```

From backend/src/main/java/com/graphify/trading/engine/RuleEvaluator.java:
```java
// 진입 평가 — volumes는 Double[] (nullable boxed)
public boolean entryTriggered(ConditionGroup entry, double[] closes, Double[] volumes, int i)

// 청산 평가 — volumes는 Double[] (nullable boxed)
public boolean exitTriggered(ExitSpec exit, double[] closes, Double[] volumes, int i, double entryPrice)

// VOLUME 지표 처리 (line 114):
case "VOLUME" -> (volumes != null && volumes[i] != null) ? volumes[i] : Double.NaN;
```

From backend/src/main/java/com/graphify/trading/backtest/BacktestService.java (현재 버그):
```java
// line 117: exitTriggered에 null volumes 전달 — BUG
if (evaluator.exitTriggered(def.exit(), closes, null, i, entryPrice))

// line 126: entryTriggered에 null volumes 전달 — BUG
if (evaluator.entryTriggered(def.entry(), closes, null, i))

// 현재 symbol 루프 (line 71-88): volumes 추출 없음
for (String symbol : symbols) {
    List<Bar> bars = filterRange(marketData.historicalDailyBars(symbol), request.from(), request.to());
    double[] closes = new double[bars.size()];
    LocalDate[] dates = new LocalDate[bars.size()];
    Map<LocalDate, Integer> idx = new HashMap<>();
    for (int i = 0; i < bars.size(); i++) {
        closes[i] = bars.get(i).close();
        dates[i] = bars.get(i).date();
        idx.put(dates[i], i);
        allDates.add(dates[i]);
    }
    closesBySymbol.put(symbol, closes);
    datesBySymbol.put(symbol, dates);
    indexBySymbol.put(symbol, idx);
}
```

**타입 주의:** closes[]는 `double[]` (primitive), volumes는 반드시 `Double[]` (nullable boxed). Bar.volume()은 `Double` nullable이므로 primitive로 선언하면 NPE 발생.
</interfaces>
</context>

<feature>
  <name>BacktestService volume null 버그 수정</name>
  <files>
    backend/src/main/java/com/graphify/trading/backtest/BacktestService.java,
    backend/src/test/java/com/graphify/trading/backtest/BacktestServiceVolumeTest.java,
    backend/src/test/java/com/graphify/trading/engine/RuleEvaluatorVolumeTest.java
  </files>
  <behavior>
    Wave 0 — 테스트 먼저 작성 (RED):

    RuleEvaluatorVolumeTest:
    - Test 1: volumes=[50000.0]이고 VOLUME > 30000 조건이면 entryTriggered()=true
    - Test 2: volumes=null이고 VOLUME > 30000 조건이면 entryTriggered()=false (NaN fallback)
    - Test 3: volumes=[10000.0]이고 VOLUME > 30000 조건이면 entryTriggered()=false

    BacktestServiceVolumeTest:
    - Test 1: market_bars에 volume=50000인 봉이 있고 VOLUME > 30000 진입 룰이면 trades에 BUY가 포함된다
    - Test 2: volumes 배열이 null이 아닌 실제 값으로 evaluator에 전달된다 (mock verify)

    Wave 1 — 구현 (GREEN):
    BacktestService.run()에서:
    1. closesBySymbol, datesBySymbol, indexBySymbol과 나란히 volumesBySymbol Map<String, Double[]> 선언
    2. symbol 루프 내부에서 closes[] 추출과 동일한 for 루프에 volumes[] 추출 추가 (bars.get(i).volume())
    3. volumesBySymbol.put(symbol, volumes) 추가
    4. 평가 루프에서 null → volumesBySymbol.getOrDefault(symbol, null) 대체
  </behavior>
  <implementation>
    1. 테스트 파일 2개 먼저 작성. 실행하면 RED여야 함 (현재 버그 때문에 Test 1이 실패).
    2. BacktestService 수정:

    변경 1 — closesBySymbol 선언 바로 아래에 추가:
    ```java
    Map<String, Double[]> volumesBySymbol = new LinkedHashMap<>();
    ```

    변경 2 — symbol 루프 내부, 기존 for (int i = 0; ...) 루프 내부에 추가:
    ```java
    Double[] volumes = new Double[bars.size()];
    // for 루프 내부에서:
    volumes[i] = bars.get(i).volume();  // Bar.volume()은 이미 Double nullable
    // for 루프 이후:
    volumesBySymbol.put(symbol, volumes);
    ```

    변경 3 — 평가 루프 (line 117):
    ```java
    Double[] vols = volumesBySymbol.getOrDefault(symbol, null);
    if (evaluator.exitTriggered(def.exit(), closes, vols, i, entryPrice))
    // ...
    if (evaluator.entryTriggered(def.entry(), closes, vols, i))
    ```

    3. 테스트 재실행 — GREEN이어야 함.
  </implementation>
</feature>

<verification>
```
./gradlew test --tests "com.graphify.trading.engine.RuleEvaluatorVolumeTest" --tests "com.graphify.trading.backtest.BacktestServiceVolumeTest" -x integrationTest
```
Two test classes, all tests GREEN.
</verification>

<success_criteria>
- RuleEvaluatorVolumeTest: volumes=[50000.0]일 때 VOLUME > 30000 조건 true 반환
- RuleEvaluatorVolumeTest: volumes=null일 때 VOLUME 조건 false 반환 (기존 동작 유지)
- BacktestServiceVolumeTest: volume=50000인 봉에서 VOLUME > 30000 룰이 BUY 신호를 생성
- `./gradlew test --tests "com.graphify.trading.backtest.*" --tests "com.graphify.trading.engine.*"` 전체 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/00-data-infra-dynamic-universe/00-01-SUMMARY.md`
</output>
