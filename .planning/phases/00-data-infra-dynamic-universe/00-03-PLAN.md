---
phase: 00-data-infra-dynamic-universe
plan: 03
type: tdd
wave: 2
depends_on:
  - 00-02
files_modified:
  - backend/src/main/java/com/graphify/market/MarketDataIngestionService.java
  - backend/src/test/java/com/graphify/market/MarketDataIngestionServiceKospi200Test.java
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "ingestDailyForKospi200()가 호출되면 in_kospi200=true인 종목만 ingestDaily()를 실행한다"
    - "in_kospi200=false인 종목은 ingestDailyForKospi200()에서 처리되지 않는다"
    - "ticker가 null인 Company는 ingestDailyForKospi200()에서 건너뛰어진다"
    - "ingestDailyForKospi200()가 적재된 종목 수를 반환한다"
  artifacts:
    - path: "backend/src/main/java/com/graphify/market/MarketDataIngestionService.java"
      provides: "ingestDailyForKospi200() 메서드"
      contains: "ingestDailyForKospi200"
    - path: "backend/src/test/java/com/graphify/market/MarketDataIngestionServiceKospi200Test.java"
      provides: "DATA-02 단위 테스트"
      exports: ["MarketDataIngestionServiceKospi200Test"]
  key_links:
    - from: "MarketDataIngestionService.ingestDailyForKospi200()"
      to: "CompanyRepository.findByInKospi200True()"
      via: "companyRepository 의존성"
      pattern: "findByInKospi200True"
    - from: "ingestDailyForKospi200()"
      to: "ingestDaily(ticker)"
      via: "company.getTicker() null 체크 후 호출"
      pattern: "ingestDaily\\(company\\.getTicker\\(\\)\\)"
---

<objective>
MarketDataIngestionService에 KOSPI 200 전체 종목 일봉 수집 메서드를 추가한다.

Purpose: DATA-01에서 만든 in_kospi200 플래그를 활용해 200개 종목의 2년치 일봉을 한 번에 적재할 수 있는 진입점을 제공한다. 기존 ingestDailyForActiveSymbols()는 룰 정의에 명시된 종목만 처리하므로 KOSPI 200 전체를 커버하지 못한다.
Output: 수정된 MarketDataIngestionService.java, MarketDataIngestionServiceKospi200Test.java
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-data-infra-dynamic-universe/00-RESEARCH.md
@.planning/phases/00-data-infra-dynamic-universe/00-02-SUMMARY.md

<interfaces>
<!-- 이 플랜이 의존하는 계약. 탐색 불필요. -->

From backend/src/main/java/com/graphify/market/MarketDataIngestionService.java (현재):
```java
@Service
public class MarketDataIngestionService {
    private final TradingRuleRepository ruleRepository;
    private final CompanyRepository companyRepository;   // 이미 주입됨
    private final YahooFinanceChartClient yahooClient;
    private final MarketBarRepository barRepository;
    private final MarketBarIntradayRepository intradayRepository;
    private final ObjectMapper objectMapper;

    /** 기존: 룰에 등장하는 전 종목 일봉 적재 */
    public int ingestDailyForActiveSymbols() { ... }

    /** 기존: 단일 종목 일봉 적재 — 이것을 재사용 */
    @Transactional
    public int ingestDaily(String symbol) { ... }
}
```

From backend/src/main/java/com/graphify/company/CompanyRepository.java (00-02에서 추가됨):
```java
List<Company> findByInKospi200True();
```

From backend/src/main/java/com/graphify/company/Company.java (00-02에서 추가됨):
```java
public boolean isInKospi200() { return inKospi200; }
public String getTicker() { return ticker; }  // nullable
```

**스케줄링 결정 (RESEARCH.md 기반):** Phase 0에서는 @Scheduled 자동 실행을 추가하지 않는다. InternalMarketController의 HTTP 엔드포인트로 수동 트리거한다. Phase 2에서 스케줄러를 추가할 때 함께 자동화한다. 이 결정을 존중하라 — @Scheduled 어노테이션 추가 금지.

**단위 테스트 전략:** ingestDaily()는 외부 Yahoo API를 호출하므로 Mockito로 mockIngestDaily를 stub한다. companyRepository.findByInKospi200True() 반환값을 제어하여 필터링 동작을 검증한다.
</interfaces>
</context>

<feature>
  <name>ingestDailyForKospi200() 메서드 추가</name>
  <files>
    backend/src/main/java/com/graphify/market/MarketDataIngestionService.java,
    backend/src/test/java/com/graphify/market/MarketDataIngestionServiceKospi200Test.java
  </files>
  <behavior>
    Wave 0 — 테스트 먼저 작성 (RED):

    MarketDataIngestionServiceKospi200Test (@ExtendWith(MockitoExtension.class)):
    - Test 1: findByInKospi200True()가 2개 종목 반환 시 ingestDaily()가 2번 호출된다
    - Test 2: ticker=null인 Company는 ingestDaily() 호출 없이 건너뛰어진다
    - Test 3: findByInKospi200True()가 빈 리스트 반환 시 반환값 0, ingestDaily() 미호출
    - Test 4: ingestDaily()가 >0 반환 시 결과 카운트에 포함된다 (반환값 검증)
    - Test 5: ingestDaily()가 0 반환 시 (데이터 없음) 카운트에 포함되지 않는다

    Wave 1 — 구현 (GREEN):
    MarketDataIngestionService에 ingestDailyForKospi200() 추가
  </behavior>
  <implementation>
    **Step 1 — 테스트 작성 (Wave 0):**

    Mockito로 companyRepository와 ingestDaily()를 mock. ingestDailyForKospi200() 메서드가 없으므로 컴파일 오류로 RED.

    **Step 2 — MarketDataIngestionService.java 수정:**

    기존 ingestDailyForActiveSymbols() 메서드 바로 아래에 추가:

    ```java
    /**
     * KOSPI 200 전체 종목 일봉 적재.
     * companies 테이블의 in_kospi200=TRUE 종목을 순회하며 ingestDaily() 호출.
     * 적재된 종목 수 반환 (ingestDaily() > 0인 종목만 카운트).
     * 스케줄 자동화는 Phase 2에서 추가 예정 — 현재는 HTTP 엔드포인트로 수동 트리거.
     */
    public int ingestDailyForKospi200() {
        List<Company> kospi200 = companyRepository.findByInKospi200True();
        int count = 0;
        for (Company company : kospi200) {
            if (company.getTicker() == null) {
                log.debug("Skip KOSPI 200 company without ticker: {}", company.getId());
                continue;
            }
            if (ingestDaily(company.getTicker()) > 0) {
                count++;
            }
        }
        log.info("KOSPI 200 daily ingestion done: {} / {} symbols ingested",
                count, kospi200.size());
        return count;
    }
    ```

    import 추가 (이미 있으면 생략):
    ```java
    import com.graphify.company.Company;
    import java.util.List;
    ```
    (Company와 List는 기존 코드에서 이미 import됨 — 확인 후 중복 추가 금지)

    **Step 3 — 테스트 재실행 GREEN 확인.**
  </implementation>
</feature>

<verification>
```
./gradlew test --tests "com.graphify.market.MarketDataIngestionServiceKospi200Test" -x integrationTest
```
All tests GREEN.

수동 검증 (선택): 애플리케이션 실행 후 HTTP POST로 수동 트리거:
```
POST /internal/market/ingest-daily-kospi200
```
(InternalMarketController에 엔드포인트가 없다면 이 플랜 scope 외 — 추가 불필요)
</verification>

<success_criteria>
- MarketDataIngestionServiceKospi200Test: 5개 테스트 모두 GREEN
- ingestDailyForKospi200()가 findByInKospi200True() 결과만 처리하고 ticker=null 종목 건너뜀
- 기존 ingestDailyForActiveSymbols() 동작 변경 없음
- `./gradlew test --tests "com.graphify.market.*"` 전체 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/00-data-infra-dynamic-universe/00-03-SUMMARY.md`
</output>
