---
phase: 06.7-volume-rank-universe
plan: 03
subsystem: market-data / volume-ranking / live-reselection
tags: [volume-ranking, live-reselection, entry-gating, position-union, design-doc]
dependency_graph:
  requires: [VolumeRankingProvider-port, YahooCumulativeVolumeAdapter, DbVolumeRankingAdapter]
  provides: [VolumeRankRefresher, LiveEvaluationService-entry-gating, DESIGN.md-v1.5.0]
  affects: [LiveDataScheduler, LiveEvaluationService, paper_live_symbols-dynamic]
tech_stack:
  added: []
  patterns: [per-rule try/catch tolerance, entrySet null=no-gate pattern, reflection id injection in tests]
key_files:
  created:
    - backend/src/main/java/com/graphify/trading/paper/VolumeRankRefresher.java
    - backend/src/test/java/com/graphify/trading/paper/VolumeRankRefresherTest.java
    - backend/src/test/java/com/graphify/trading/paper/LiveEvaluationServiceVolumeTest.java
  modified:
    - backend/src/main/java/com/graphify/market/LiveDataScheduler.java
    - backend/src/main/java/com/graphify/trading/paper/LiveEvaluationService.java
    - backend/src/test/java/com/graphify/market/LiveDataSchedulerTest.java
    - backend/src/test/java/com/graphify/trading/paper/LiveEvaluationServiceTest.java
    - DESIGN.md
decisions:
  - "buildEntrySet() returns null for non-volume_top_n rules — null means no gating, avoids boolean flag proliferation"
  - "entry gate fires in evaluateSymbol() AFTER positionRepo.findByAccountIdAndSymbol (position check first, gate in empty-position branch)"
  - "VolumeRankRefresher per-rule try/catch + log.warn — one rule parse/fetch failure does not block other rules or the full tick"
  - "DESIGN.md v1.5.0 added as new top-level entry (single-doc rule) — no new per-feature md created"
  - "PaperAccount.id set via reflection in tests — DB-generated id is null without persist; reflection injection matches TradingRule test pattern"
metrics:
  duration: 8m
  completed_date: "2026-06-23"
  tasks_completed: 3
  tasks_total: 3
  files_created: 3
  files_modified: 5
---

# Phase 6.7 Plan 03: VolumeRankRefresher + Entry Gating + DESIGN.md Summary

**One-liner:** VolumeRankRefresher (top-N ∪ held-position union) + LiveEvaluationService entry gating (top-N members only) + exit always for held symbols + DESIGN.md v1.5.0 backtest↔live consistency policy documented.

## Tasks Executed

| # | Name | Status | Commit |
|---|------|--------|--------|
| 1 | VolumeRankRefresher — 매 틱 재선정 + 보유 포지션 union | Completed (TDD green 4/4) | 74c6ce8 |
| 2 | LiveDataScheduler 연결 + LiveEvaluationService 진입 게이팅 | Completed (TDD green 4/4 + regression green) | a98211c |
| 3 | DESIGN.md 일관성 방침 문서화 (SC5) | Completed | bc5ed0f |

## Implementation Details

### Task 1: VolumeRankRefresher

**File:** `backend/src/main/java/com/graphify/trading/paper/VolumeRankRefresher.java`

- `@Component` — `@Qualifier("yahooCumulativeVolumeAdapter") VolumeRankingProvider` 주입
- `refreshIfVolumeTopN(LocalDate today)`:
  - RUNNING 룰 순회 → `universe.type == "volume_top_n"`인 룰만 처리 (Pitfall 3)
  - `liveRanking.topVolume(market, today, topN, true)` → `LinkedHashSet` 초기화
  - `additionalSymbols` 있으면 합산
  - `accountRepo.findByUserId()` → `positionRepo.findByAccountId()` 보유 포지션 symbol 합산 (Pitfall 2, SC4)
  - `paperLiveSymbolService.assignSymbols(ruleId, merged)` — delete+insert 교체
  - per-rule try/catch + log.warn (전체 틱 중단 방지)

**Test results (VolumeRankRefresherTest):**
```
✅ refreshIfVolumeTopN_onlyAssignsSymbolsForVolumeTopNRules_notSymbolsTypeRules  (SC4/Pitfall 3)
✅ refreshIfVolumeTopN_mergesHeldPositionSymbols_whenDroppedFromTopN             (SC4/SC5)
✅ refreshIfVolumeTopN_noDuplicates_whenTopNAndPositionsOverlap                  (LinkedHashSet)
✅ refreshIfVolumeTopN_noAccount_assignsOnlyTopN_withoutNPE                      (NPE 안전)
```

### Task 2: LiveDataScheduler + LiveEvaluationService

**LiveDataScheduler changes:**
- `VolumeRankRefresher volumeRankRefresher` 필드 + 생성자 주입
- `collectLiveData()`: Guard 2 직후, `activeSymbolsUnion()` 직전에 `volumeRankRefresher.refreshIfVolumeTopN(now.toLocalDate())` 1줄 추가

**LiveEvaluationService changes:**
- `@Qualifier("yahooCumulativeVolumeAdapter") VolumeRankingProvider liveRanking` 생성자 주입
- `buildEntrySet(RuleDefinition def, Instant tickTime)` 신규:
  - `universe.type != "volume_top_n"` → null 반환 (게이팅 없음)
  - volume_top_n이면 `liveRanking.topVolume(market, today, topN, true)` → `HashSet` 반환
  - 예외 시 null 반환 (평가 자체 중단 방지)
- `evaluateRule()`: `buildEntrySet()` 1회 호출 → `evaluateSymbol()`에 전달
- `evaluateSymbol()` 시그니처 변경: `Set<String> entrySet` 파라미터 추가
  - `posOpt.isPresent()` 분기 (EXIT): 변경 없음 — 항상 청산 평가 (SC4)
  - `posOpt.isEmpty()` 분기 (ENTRY): `entrySet != null && !entrySet.contains(symbol)` → return (진입 차단)

**Test results (LiveEvaluationServiceVolumeTest):**
```
✅ volumeTopN_exitEvaluated_evenWhenSymbolDroppedFromTopN  (SC5b — 이탈 종목 청산 유지)
✅ volumeTopN_entryBlocked_whenSymbolNotInTopN             (SC4 — 비-top-N 진입 차단)
✅ volumeTopN_entryAllowed_whenSymbolIsInTopN              (SC4 — top-N 멤버 진입 허용)
✅ symbolsRule_entryNotGated_byTopN                        (Pitfall 3 — symbols 룰 게이팅 없음)
```

**Regression results:**
```
LiveEvaluationServiceTest: 5/5 green (기존 테스트 회귀 없음)
LiveDataSchedulerTest: 5/5 green (기존 테스트 회귀 없음)
Full ./gradlew test: BUILD SUCCESSFUL
```

### Task 3: DESIGN.md v1.5.0

DESIGN.md 최상단에 `## [v1.5.0] 실시간 거래량 상위 유니버스 (VolumeRankingProvider)` 항목 추가.

포함 내용:
- VolumeRankingProvider 포트 + 3개 구현체 구조 요약
- 백테스트(완결 일봉)↔라이브(Yahoo 5m 누적) 기준 차이 표 + 일관성 방침 (Deferred 명시)
- 라이브 재선정 동작: SC3(매 틱), SC4(보유 유지), 진입/청산 분리
- ETF/ETN 제외: instrument_type='COMMON_STOCK' (백테스트·라이브 동일)
- 캐시 설계(1분 TTL, empty-no-cache), KRX deferred 항목
- "UI/UX: 해당 없음 — 백엔드 데이터 인프라 페이즈"

## Success Criteria Verification

| Criterion | Status |
|-----------|--------|
| SC3: 라이브 volume_top_n 매 틱 재선정 (승격 고정 X) | PASS — VolumeRankRefresher.refreshIfVolumeTopN() |
| SC4: top-N 이탈 + 보유 포지션 청산 평가 계속 (진입=top-N, 청산=보유) | PASS — union in refresher + entry gate in evaluateSymbol |
| SC5: 백테스트↔라이브 기준 차이 + 일관성 방침 DESIGN.md 문서화 | PASS — DESIGN.md v1.5.0 |
| SC5b: LiveEvaluationService top-N 이탈 + 포지션 보유 SELL 호출 | PASS — Test 1 |
| Pitfall 3: symbols/watchlist 룰 재선정 미적용 | PASS — Test 1 (VolumeRankRefresherTest) + Test 4 (VolumeTest) |
| 전체 테스트 그린 — DATA-06 종결 | PASS — BUILD SUCCESSFUL |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] PaperAccount.id is null in tests (DB-generated, not set by constructor)**

- **Found during:** Task 2 test failures
- **Issue:** `PaperAccount(userId, cash)` constructor doesn't set `id` (auto-generated). Tests stubbed `positionRepo.findByAccountIdAndSymbol(1L, ...)` but service called with `null` → `UnnecessaryStubbingException` / mismatches.
- **Fix:** Added `makeAccount()` helper using reflection (`PaperAccount.class.getDeclaredField("id").set(acc, 1L)`) — same pattern as `TradingRule.id` injection in existing `LiveEvaluationServiceTest`.
- **Files modified:** `LiveEvaluationServiceVolumeTest.java`

**2. [Rule 1 - Bug] UnnecessaryStubbingException in entry-blocked test**

- **Found during:** Task 2 Test 2 (`volumeTopN_entryBlocked_whenSymbolNotInTopN`)
- **Issue:** Stubbed `ruleEvaluator.evalEntry()` but it's never called — entry gate fires (returns) BEFORE evalEntry in the entry branch.
- **Fix:** Removed the `ruleEvaluator.evalEntry` stub from that test. Comment added explaining the gate fires before evalEntry.
- **Files modified:** `LiveEvaluationServiceVolumeTest.java`

**3. [Rule 3 - Blocking] Existing tests broke after constructor changes**

- **Found during:** Task 2 implementation — adding `VolumeRankingProvider` to `LiveEvaluationService` + `VolumeRankRefresher` to `LiveDataScheduler` constructors.
- **Fix:** Updated `LiveEvaluationServiceTest.setUp()` (added `liveRanking` mock + import) and `LiveDataSchedulerTest.setUp()` (added `volumeRankRefresher` mock + import).
- **Files modified:** `LiveEvaluationServiceTest.java`, `LiveDataSchedulerTest.java`

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| VolumeRankRefresher.java exists | FOUND |
| VolumeRankRefresherTest.java exists | FOUND |
| LiveEvaluationServiceVolumeTest.java exists | FOUND |
| Commit 74c6ce8 (VolumeRankRefresher) | FOUND |
| Commit a98211c (LiveDataScheduler + LiveEvaluationService) | FOUND |
| Commit bc5ed0f (DESIGN.md) | FOUND |
| VolumeRankRefresherTest 4/4 | GREEN |
| LiveEvaluationServiceVolumeTest 4/4 | GREEN |
| LiveEvaluationServiceTest 5/5 (regression) | GREEN |
| LiveDataSchedulerTest 5/5 (regression) | GREEN |
| Full ./gradlew test | BUILD SUCCESSFUL |
| DESIGN.md grep VolumeRankingProvider | FOUND |
| DESIGN.md grep COMMON_STOCK | FOUND |
