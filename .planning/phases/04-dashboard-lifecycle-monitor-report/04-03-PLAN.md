---
phase: 04-dashboard-lifecycle-monitor-report
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04

must_haves:
  truths:
    - "GET /api/v1/trading/paper/monitor returns schedulerLastRun, marketStatus, recentSignals (last 50), todayTrades"
    - "TradingMonitorPage renders scheduler last run time, market open/closed indicator, signal log table, today's trade feed"
    - "Signal log table shows: ts, symbol, signal (BUY/SELL/HOLD badge), executed boolean, indicator_snapshot (RSI, SMA20, price)"
    - "TradingMonitorPage polls every 30 seconds via useQuery refetchInterval"
  artifacts:
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperMonitorController.java"
      provides: "GET /api/v1/trading/paper/monitor"
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperMonitorService.java"
      provides: "aggregates scheduler state + recent signals + today trades"
    - path: "backend/src/main/java/com/graphify/trading/paper/dto/MonitorDto.java"
      provides: "response DTO with schedulerLastRun, marketStatus, signals, trades"
    - path: "frontend/src/pages/trading/TradingMonitorPage.tsx"
      provides: "monitor UI with auto-refresh"
---

<objective>
Build the trading monitor page: backend API aggregating scheduler state, market open/closed status, recent signal evaluations, and today's trades; frontend page with 30-second auto-refresh showing signal log table with indicator snapshots.
</objective>

<context>
@.planning/phases/04-dashboard-lifecycle-monitor-report/04-CONTEXT.md

Key data sources:
- `PaperSignalLogRepository.findTop50ByOrderByTsDesc()` — recent 50 signals
- `PaperTradeRepository.findByAccountIdOrderByTradedAtDesc(accountId)` — filter to today
- Market status: check current KST time (09:00–15:30 weekday = OPEN, otherwise CLOSED)
- Scheduler last run: `PaperSignalLog` max `created_at` as proxy (no separate scheduler_run table)
- indicator_snapshot is stored as JSON string — parse to expose rsi14, sma20, price
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monitor backend (DTO + Service + Controller)</name>
  <files>
    backend/src/main/java/com/graphify/trading/paper/dto/SignalLogItem.java,
    backend/src/main/java/com/graphify/trading/paper/dto/TradeItem.java,
    backend/src/main/java/com/graphify/trading/paper/dto/MonitorDto.java,
    backend/src/main/java/com/graphify/trading/paper/PaperMonitorService.java,
    backend/src/main/java/com/graphify/trading/paper/PaperMonitorController.java
  </files>
  <action>
1. Create `SignalLogItem.java` record:
```java
public record SignalLogItem(
    Long id, Long ruleId, String symbol, Instant ts,
    String signal, boolean executed,
    Double rsi14, Double sma20, Double price  // parsed from indicator_snapshot
) {}
```

2. Create `TradeItem.java` record:
```java
public record TradeItem(
    Long id, String symbol, String side, double qty,
    double price, Double pnl, Instant tradedAt
) {}
```

3. Create `MonitorDto.java` record:
```java
public record MonitorDto(
    Instant schedulerLastRun,   // max created_at from paper_signal_log, or null
    String marketStatus,        // "OPEN" | "CLOSED"
    List<SignalLogItem> recentSignals,
    List<TradeItem> todayTrades
) {}
```

4. Create `PaperMonitorService.java`:
- `@Service @Transactional(readOnly=true)`
- `getMonitor(Long userId)`:
  - Get account (optional — if none, return empty lists)
  - `schedulerLastRun`: `signalLogRepo.findTop50ByOrderByTsDesc()` → stream max `ts` → or null
  - `marketStatus`: check current KST ZonedDateTime — weekday 09:00–15:30 = "OPEN" else "CLOSED"
  - `recentSignals`: `signalLogRepo.findTop50ByOrderByTsDesc()` — parse `indicator_snapshot` JSON for each, extract rsi14/sma20/price; handle parse errors gracefully (nulls)
  - `todayTrades`: `tradeRepo.findByAccountIdOrderByTradedAtDesc(accountId)` — filter to today KST midnight onwards

5. Create `PaperMonitorController.java`:
- `@RestController @RequestMapping("/api/v1/trading/paper/monitor")`
- `@GetMapping` → returns `ApiResponse<MonitorDto>`

6. `./gradlew compileJava`
  </action>
  <verify>
    <automated>cd /Users/heojeonghun/Desktop/New_Graph/backend && ./gradlew compileJava 2>&1 | tail -5</automated>
  </verify>
  <done>compileJava succeeds; monitor endpoint exists</done>
</task>

<task type="auto">
  <name>Task 2: TradingMonitorPage frontend</name>
  <files>
    frontend/src/lib/paperApi.ts,
    frontend/src/types/paper.ts,
    frontend/src/pages/trading/TradingMonitorPage.tsx
  </files>
  <action>
1. Add to `frontend/src/types/paper.ts`:
```typescript
export interface SignalLogItem {
  id: number;
  ruleId: number;
  symbol: string;
  ts: string;
  signal: "BUY" | "SELL" | "HOLD";
  executed: boolean;
  rsi14: number | null;
  sma20: number | null;
  price: number | null;
}

export interface TradeItem {
  id: number;
  symbol: string;
  side: "BUY" | "SELL";
  qty: number;
  price: number;
  pnl: number | null;
  tradedAt: string;
}

export interface MonitorData {
  schedulerLastRun: string | null;
  marketStatus: "OPEN" | "CLOSED";
  recentSignals: SignalLogItem[];
  todayTrades: TradeItem[];
}
```

2. Add to `frontend/src/lib/paperApi.ts`:
```typescript
export async function fetchPaperMonitor() {
  return apiGet<MonitorData>(`${BASE}/monitor`);
}
```

3. Implement `TradingMonitorPage.tsx`:
- `useQuery({ queryKey: ["trading","paper","monitor"], queryFn: ..., refetchInterval: 30000 })`
- Top row: market status badge (OPEN=green, CLOSED=gray) + scheduler last run time (formatted as "YY.MM.DD HH:mm KST")
- Signal log table: 시각 | 종목 | 신호(BUY=green/SELL=red/HOLD=gray badge) | 체결여부 | RSI14 | SMA20 | 현재가
- Today's trades section: 시각 | 종목 | 매수/매도 | 수량 | 체결가 | 손익
- Empty states for each section
- 30-second countdown or last-updated timestamp shown
  </action>
  <verify>
    <automated>cd /Users/heojeonghun/Desktop/New_Graph/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>TypeScript compiles; TradingMonitorPage renders monitor UI</done>
</task>

</tasks>

<verification>
1. `./gradlew compileJava` — no errors
2. `npx tsc --noEmit` — no errors
3. Monitor endpoint at GET /api/v1/trading/paper/monitor
</verification>

<success_criteria>
- MonitorDto: schedulerLastRun, marketStatus, recentSignals (50), todayTrades
- Signal log includes parsed rsi14/sma20/price from indicator_snapshot JSON
- TradingMonitorPage: market status, scheduler last run, signal log table, today's trade feed
- 30s auto-refresh via refetchInterval
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-lifecycle-monitor-report/04-03-SUMMARY.md`
</output>
