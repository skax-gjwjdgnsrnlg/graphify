---
phase: 06.8-trading-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: ["06.8-01"]
files_modified:
  - frontend/src/components/trading/TradingCompanyPickerModal.tsx
  - frontend/src/pages/trading/paper/PaperRulesPage.tsx
  - frontend/src/pages/trading/TradingRulesPage.tsx
  - frontend/src/components/trading/TradeRationaleRow.tsx
  - frontend/src/pages/trading/TradingRulesEditPage.tsx
autonomous: true
requirements: [UIX-01]
user_setup: []

must_haves:
  truths:
    - "전략 설정 (PaperRulesPage) lists all rules, shows DRAFT/ACTIVE config-axis badge, and keeps the RUNNING-rule GUARD (하향·편집 disabled with tooltip)"
    - "전략 운영 (TradingRulesPage) shows ACTIVE rules only with STOPPED↔RUNNING control (시작/중지) preserved"
    - "룰 빌더 (TradingRulesEditPage) keeps its 6-section builder + JSON tab two-way binding and validation; only theme/layout changes"
    - "Stock-picker modal renders in trade dark theme (no cream PrimaryButton/TextField bleed-through)"
    - "All rule CRUD, 2-axis state transitions, and validation behavior are unchanged"
  artifacts:
    - path: "frontend/src/components/trading/TradingCompanyPickerModal.tsx"
      provides: "Trade-themed stock picker (clone of shared/CompanyPickerModal props + search logic, trade primitives for UI)"
      min_lines: 40
    - path: "frontend/src/pages/trading/paper/PaperRulesPage.tsx"
      provides: "전략 설정 — rule list + config-axis badges + GUARD"
      contains: "TradeBadge"
    - path: "frontend/src/pages/trading/TradingRulesPage.tsx"
      provides: "전략 운영 — ACTIVE filter + run-axis start/stop"
      contains: "TradeBadge"
    - path: "frontend/src/pages/trading/TradingRulesEditPage.tsx"
      provides: "룰 빌더 reskin/relayout (builder + JSON tabs)"
      contains: "TradeButton"
  key_links:
    - from: "frontend/src/pages/trading/paper/PaperRulesPage.tsx"
      to: "frontend/src/components/trading/ui"
      via: "import trade primitives"
      pattern: "components/trading/ui"
    - from: "frontend/src/components/trading/TradingCompanyPickerModal.tsx"
      to: "frontend/src/components/trading/ui"
      via: "TradeButton/TradeInput instead of cream shared/"
      pattern: "components/trading/ui"
---

<objective>
Reskin the rule cluster to the Binance dark theme: 전략 설정 (PaperRulesPage), 전략 운영 (TradingRulesPage), the 838-line 룰 빌더 (TradingRulesEditPage builder + JSON tabs), the rule-rationale row, and a new trade-themed stock-picker modal. All consume the Wave 1 primitives.

Purpose: This is the most behavior-critical cluster (2-axis state model, GUARD logic, validation). The reskin must be purely presentational so the DRAFT↔ACTIVE / STOPPED↔RUNNING semantics and form validation stay intact.
Output: Trade-themed rule screens matching wireframe sections "전략 설정", "전략 운영", "룰 빌더", and the stock-picker modal.

Behavior unchanged: No backend/REST/DTO edits. No chart edits. Rule CRUD endpoints, lifecycle endpoints, toDefinition/fromDefinition serialization, formError/jsonError validation, and the RUNNING-rule transition GUARD are all preserved — only Tailwind classes and component swaps change. Korean status/error strings stay byte-identical.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.8-trading-ui-redesign/06.8-RESEARCH.md
@.planning/phases/06.8-trading-ui-redesign/06.8-01-SUMMARY.md
@wireframe.html
@frontend/src/components/shared/CompanyPickerModal.tsx
@frontend/src/pages/trading/paper/PaperRulesPage.tsx
@frontend/src/pages/trading/TradingRulesPage.tsx
@frontend/src/pages/trading/TradingRulesEditPage.tsx
@frontend/src/components/trading/TradeRationaleRow.tsx

<interfaces>
<!-- Wave 1 primitives (components/trading/ui/index.ts) — see 06.8-01-SUMMARY.md for exact props -->
TradeButton(variant, size, disabled, loading, onClick), TradeBadge(variant),
TradeCard(title?), TradeStatCard(label,value,sub?,valueColor?),
TradeTable + TradeTableRow + TradeTableHeader, TradePageState(variant,title?,message?,onRetry?),
TradeInput(mono?, ...inputProps), TradeModeIndicator.

<!-- Token reference: trade-up/down for badges, trade-primary for primary CTAs,
     trade-up-soft/down-soft/elevated for badge backgrounds, font-trade-mono for cooldown/dates numerics. -->

<!-- Wireframe anchors: 전략 설정 lines ~319–352, 전략 운영 lines ~354–378,
     종목 선택 모달 lines ~380–398, 룰 빌더 빌더탭 lines ~400–447, JSON탭 lines ~449–471. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TradingCompanyPickerModal (trade-themed) + reskin TradeRationaleRow</name>
  <files>
    frontend/src/components/trading/TradingCompanyPickerModal.tsx,
    frontend/src/components/trading/TradeRationaleRow.tsx
  </files>
  <action>
Per RESEARCH Open Q1 + D6 (shared/ untouched), create `TradingCompanyPickerModal.tsx`: copy the exact props interface and search/selection logic from `components/shared/CompanyPickerModal.tsx`, but replace every cream shared/ element (PrimaryButton, GhostButton, TextField, EmptyState, InlineError, SkeletonBlock, Pagination) with trade equivalents:
- search field → TradeInput; selected chips → trade-primary pill (bg-trade-primary text-trade-ink rounded-full) with ✕; candidate chips → border-trade-hairline text-trade-muted-strong; primary "선택 적용" → TradeButton primary; "취소" → TradeButton secondary; loading/empty/error → TradePageState; pagination → reskinned inline buttons (trade tokens).
- Modal shell: overlay bg-black/60; panel bg-trade-surface border-trade-hairline rounded-xl. Match wireframe lines ~380–398.
- Keep the same exported component signature so consumers swap import path only. Do NOT modify shared/CompanyPickerModal.tsx.

TradeRationaleRow.tsx (reskin only — preserve the accordion data logic): swap bg-gray-800/40→bg-trade-elevated/40, text-emerald-400→text-trade-up, text-red-400/500→text-trade-down, text-gray-400→text-trade-muted, border-white/*→border-trade-hairline. Trigger condition ✓/✗ markers use trade-up/trade-down. Keep all Korean strings ("청산 사유", "근거 정보 없음", etc.).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|border-white/" frontend/src/components/trading/TradingCompanyPickerModal.tsx frontend/src/components/trading/TradeRationaleRow.tsx
  </verify>
  <done>TradingCompanyPickerModal compiles with same props as shared original; shared/CompanyPickerModal.tsx unchanged (git diff empty). TradeRationaleRow reskinned, accordion logic intact, strings preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin 전략 설정 (PaperRulesPage) + 전략 운영 (TradingRulesPage)</name>
  <files>
    frontend/src/pages/trading/paper/PaperRulesPage.tsx,
    frontend/src/pages/trading/TradingRulesPage.tsx
  </files>
  <action>
PaperRulesPage (전략 설정 — wireframe ~319–352): reskin the page chrome + rule table.
- Header: title "전략 설정" text-trade-on-dark + sub "룰 CRUD · 설정 축(DRAFT ↔ ACTIVE)" text-trade-muted; "+ 새 룰" → TradeButton primary.
- Failure banner (활성화/하향 실패): bg-trade-down-soft border-trade-down/30 text-trade-down (inline).
- Table → TradeTable/TradeTableRow keeping existing column grid (이름 | 설정 상태 | 쿨다운 | 수정일 | 관리). Config-axis badge via TradeBadge variant=active (ACTIVE) / draft (DRAFT). Cooldown + 수정일 cells → font-trade-mono.
- GUARD (CRITICAL — preserve logic): RUNNING rules keep 하향/편집 disabled + tooltip "전략 운영에서 먼저 중지". Disabled action text → text-trade-muted (wireframe uses dim). Action links 활성화/하향/편집/복제/삭제 keep existing handlers; primary action color text-trade-primary or text-trade-up per existing semantics.
- EMPTY/ERROR/LOADING → TradePageState with existing Korean strings ("등록된 룰이 없습니다…", etc.).

TradingRulesPage (전략 운영 — wireframe ~354–378): this page is shared by PAPER (/paper/rules-lifecycle) and LIVE (/rules) routes — reskin once.
- Header: "전략 운영" + sub "ACTIVE 룰만 표시 · 실행 축(STOPPED ↔ RUNNING)".
- Table grid (룰 이름 | 실행 상태 | 수정일 | 제어). Run-axis badge: RUNNING → TradeBadge variant=running ("● 실행 중"), STOPPED → variant=stopped ("중지됨"). Control buttons: 중지 → TradeButton danger; 시작 → TradeButton with bg-trade-up (use a green control — per wireframe 시작 is green #0ecb81; implement as TradeButton variant with text-trade-on-dark on bg-trade-up, or inline trade-up button). 복사 → ghost. 승격(Later) → text-trade-primary disabled.
- Preserve ACTIVE filtering + start/stop mutation handlers exactly. EMPTY → TradePageState "ACTIVE 상태인 룰이 없습니다. 전략 설정에서 활성화하세요."
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/pages/trading/paper/PaperRulesPage.tsx frontend/src/pages/trading/TradingRulesPage.tsx
  </verify>
  <done>Both pages compile and match wireframe. Config-axis vs run-axis badges correct. RUNNING GUARD (disable + tooltip) preserved. ACTIVE filter + start/stop handlers unchanged. All Korean strings intact.</done>
</task>

<task type="auto">
  <name>Task 3: Relayout 룰 빌더 (TradingRulesEditPage — builder + JSON tabs)</name>
  <files>frontend/src/pages/trading/TradingRulesEditPage.tsx</files>
  <action>
Reskin/relayout the 838-line builder to wireframe sections (~400–471). This is the largest file — work section by section, swap tokens, do NOT alter toDefinition/fromDefinition, the builder↔JSON two-way sync, or validation.
- Top bar: "← 돌아가기" text-trade-muted + title (새 룰 / 룰 편집) text-trade-on-dark.
- 빌더/JSON tab control: segmented pill (bg-trade-surface p-1 rounded-lg); active tab bg-trade-primary text-trade-ink, inactive text-trade-muted.
- 6 sections (① 룰 이름, ② 유니버스, ③ 진입 조건 [logic AND/OR], ④ 청산 조건 [익절%/손절%/지표], ⑤ 사이징, ⑥ 제약 [쿨다운/최대]): section labels text-xs font-semibold text-trade-muted; all inputs/selects → TradeInput / trade-styled selects (bg-trade-surface text-trade-body border-trade-hairline); numeric values font-trade-mono; 익절 value text-trade-up, 손절 value text-trade-down (per wireframe). "+ 조건 추가"/"+ 지표 조건"/"+ 추가종목" → text-trade-primary action links. Universe radio options reskinned; direct-symbol selection opens TradingCompanyPickerModal (from Task 1) if that flow currently exists — swap shared import → trade import; otherwise leave the existing picker wiring but themed.
- JSON tab: code panel bg-trade-surface; <pre> font-trade-mono text-trade-muted-strong; jsonError text-trade-down "JSON 형식이 올바르지 않습니다".
- Footer: formError text-trade-down ("이름 필수 · 진입조건 ≥1 · 사이징 숫자"); 취소 → TradeButton secondary; 저장 → TradeButton primary (loading state during save).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/pages/trading/TradingRulesEditPage.tsx
  </verify>
  <done>Builder compiles; both tabs themed; two-way builder↔JSON binding + toDefinition/fromDefinition + formError/jsonError validation all unchanged; direct-symbol picker uses TradingCompanyPickerModal (if wired). No cream classes/hex remain.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` passes.
- Token guards (c-1/c-2) over all 5 files: 0 hex, 0 cream classes. shared/CompanyPickerModal.tsx git diff empty (D6).
- Visual (browse + visual-verdict): 전략 설정, 전략 운영, 룰 빌더 (both tabs), stock-picker modal vs wireframe sections.
- GUARD behavior: in 전략 설정, a RUNNING rule shows 하향/편집 disabled + tooltip. 2-axis badges distinct between the two pages.
</verification>

<success_criteria>
SC1 (tokens), SC2 (rule CRUD + 2-axis state + GUARD + validation + modal preserved), SC4 (empty/loading/error states), SC6 (D4 labels 전략 설정/전략 운영) satisfied for the rule cluster.
</success_criteria>

<output>
After completion, create `.planning/phases/06.8-trading-ui-redesign/06.8-02-SUMMARY.md` noting the TradingCompanyPickerModal import path + props (so 06.8-04 can reuse it for the empty-universe fallback).
</output>
