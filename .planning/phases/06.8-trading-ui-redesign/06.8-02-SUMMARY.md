---
phase: 06.8-trading-ui-redesign
plan: "02"
subsystem: frontend-trading-ui
tags: [rule-cluster, TradingCompanyPickerModal, PaperRulesPage, TradingRulesPage, TradingRulesEditPage, TradeRationaleRow, Binance-dark-theme, UIX-01]
dependency_graph:
  requires:
    - components/trading/ui/ (06.8-01 — TradeButton, TradeBadge, TradeTable, TradePageState, TradeInput)
  provides:
    - TradingCompanyPickerModal (trade-themed picker; import path for 06.8-04 empty-universe fallback)
    - PaperRulesPage reskinned (config-axis DRAFT/ACTIVE badges, GUARD preserved)
    - TradingRulesPage reskinned (run-axis STOPPED/RUNNING badges, picker swapped)
    - TradingRulesEditPage reskinned (6-section builder + JSON tabs, segmented pill tab)
    - TradeRationaleRow reskinned (accordion logic intact)
  affects:
    - 06.8-04 (TradingCompanyPickerModal reuse for empty-universe fallback)
tech_stack:
  added: []
  patterns:
    - trade-* token-only styling (0 hardcoded hex, 0 cream classes in all 5 files)
    - TradeTable grid-cols arbitrary values (grid-cols-[2fr_1fr_1fr_1fr_1.4fr])
    - segmented pill tab control (bg-trade-surface p-1 rounded-lg)
    - INPUT_CLS/SELECT_CLS constants for consistent input styling
key_files:
  created:
    - frontend/src/components/trading/TradingCompanyPickerModal.tsx
  modified:
    - frontend/src/components/trading/TradeRationaleRow.tsx
    - frontend/src/pages/trading/paper/PaperRulesPage.tsx
    - frontend/src/pages/trading/TradingRulesPage.tsx
    - frontend/src/pages/trading/TradingRulesEditPage.tsx
decisions:
  - "TradingCompanyPickerModal props interface byte-identical to shared/CompanyPickerModal — consumers swap import path only; shared/CompanyPickerModal.tsx untouched (D6)"
  - "시작 button in TradingRulesPage uses inline bg-trade-up button (not TradeButton variant) — TradeButton has no green variant; avoids adding new variant to primitive catalog"
  - "RUNNING indicator (●실행중) added inline to PaperRulesPage rule name cell — matches wireframe line 339 visual; purely presentational"
  - "益절/손절 inputs use text-trade-up / text-trade-down with font-trade-mono — per wireframe color semantics; input value colorization via text class"
  - "TradeTable grid arbitrary values (grid-cols-[2fr_1fr_1fr_1fr_1.4fr]) used instead of HTML table — matches primitive catalog pattern from 06.8-01"
metrics:
  duration: "8m"
  completed_date: "2026-06-28"
  tasks_completed: 3
  files_created: 1
  files_modified: 4
---

# Phase 6.8 Plan 02: Rule Cluster Reskin Summary

**One-liner:** Rule cluster reskinned to Binance dark — trade-themed CompanyPickerModal, PaperRulesPage (config-axis GUARD preserved), TradingRulesPage (run-axis + TradingCompanyPickerModal swap), TradingRulesEditPage (segmented-pill tabs + token inputs), TradeRationaleRow; all 2-axis state/CRUD/validation behavior byte-identical.

## TradingCompanyPickerModal — Import Path (for 06.8-04)

```tsx
// 06.8-04 empty-universe fallback: swap this import
import { TradingCompanyPickerModal } from "@/components/trading/TradingCompanyPickerModal";

// Props (identical to shared/CompanyPickerModal):
<TradingCompanyPickerModal
  open={boolean}
  title="종목 직접 선택"          // optional, default shown
  description="폴백 원인 메시지"  // optional
  confirmLabel="선택 적용"        // optional
  onConfirm={(symbols: string[]) => void}
  onClose={() => void}
/>
```

Modal shell: `bg-trade-surface border-trade-hairline rounded-xl`, overlay `bg-black/60`.
Selected chips: `bg-trade-primary text-trade-ink rounded-full`.
Candidate chips: `border-trade-hairline text-trade-muted-strong`.

## Behavioral Invariants (unchanged)

| Feature | Status |
|---------|--------|
| Rule CRUD (create/activate/deactivate/copy/delete) | PRESERVED |
| 2-axis state model (DRAFT↔ACTIVE / STOPPED↔RUNNING) | PRESERVED |
| RUNNING GUARD (하향/편집 disabled + tooltip "전략 운영에서 먼저 중지") | PRESERVED |
| ACTIVE filter in TradingRulesPage | PRESERVED |
| start/stop mutation handlers + ERR_LIFECYCLE_005 fallback | PRESERVED |
| toDefinition/fromDefinition two-way serialization | PRESERVED |
| builder↔JSON tab sync | PRESERVED |
| formError/jsonError validation messages | PRESERVED |
| Korean strings (청산 사유, 근거 정보 없음, ACTIVE 상태인 룰이 없습니다…) | PRESERVED |

## Verification Results

| Check | Result |
|-------|--------|
| `npm run build` (tsc -b + vite) | PASS |
| Token guard — all 5 files (0 hex, 0 cream/gray/emerald classes) | PASS |
| D6 guard — shared/CompanyPickerModal.tsx git diff empty | PASS |
| RUNNING GUARD logic (하향/편집 disabled + tooltip) | PASS (code review) |
| ACTIVE filter in TradingRulesPage | PASS (code review) |
| ERR_LIFECYCLE_005 fallback → TradingCompanyPickerModal | PASS (code review) |

## Commits

| Hash | Description |
|------|-------------|
| `250fc0d` | feat(06.8-02): trade-themed TradingCompanyPickerModal + reskin TradeRationaleRow |
| `e36fc61` | feat(06.8-02): reskin 전략설정 (PaperRulesPage) + 전략운영 (TradingRulesPage) |
| `6414680` | feat(06.8-02): relayout 룰 빌더 (TradingRulesEditPage — builder + JSON tabs) |

## Deviations from Plan

### Auto-fixed Issues

None.

### Notes

**시작 button (TradingRulesPage):** Plan specified "TradeButton with bg-trade-up or inline trade-up button". Used an inline `<button>` with `bg-trade-up text-trade-on-dark` rather than TradeButton, since TradeButton has no green variant. Keeps primitive catalog clean (no new variant needed for this single use-case).

**RUNNING indicator:** Added `●실행중` inline badge on rule name in PaperRulesPage (wireframe line 339). Purely visual; no behavior added.

**Parallel execution:** 06.8-03 and 06.8-05 agents ran concurrently. No file conflicts — each plan touched different files. Interleaved commits are normal for wave-parallel execution.

## Self-Check: PASSED
