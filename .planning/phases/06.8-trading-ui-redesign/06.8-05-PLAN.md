---
phase: 06.8-trading-ui-redesign
plan: 05
type: execute
wave: 5
depends_on: ["06.8-01"]
files_modified:
  - frontend/src/pages/trading/TradingChatPage.tsx
  - frontend/src/pages/trading/TossSettingsPage.tsx
  - frontend/src/pages/trading/TradingDashboardPage.tsx
  - frontend/src/pages/trading/TradingHistoryPage.tsx
autonomous: true
requirements: [UIX-01]
user_setup: []

must_haves:
  truths:
    - "DDS Agent chat is reskinned to the Binance dark theme, mockup behavior (setTimeout) preserved (D5)"
    - "토스 설정 shows the 3-way status badge (미설정 / 설정됨·유효 / 설정됨·만료) and save/refresh flow themed"
    - "LIVE 대시보드 + 거래 이력 are disabled coming-soon placeholders with a 서킷 브레이커 banner slot (D7)"
    - "Phase 7/8 visual slots (TV 배지, webhook URL copy, 승격 게이트 checklist) exist as disabled placeholders only — no real implementation"
    - "No backend calls or behavior added for placeholders; cream app unaffected"
  artifacts:
    - path: "frontend/src/pages/trading/TradingChatPage.tsx"
      provides: "Reskinned DDS Agent chat (mockup kept)"
      contains: "font-trade"
    - path: "frontend/src/pages/trading/TossSettingsPage.tsx"
      provides: "Themed Toss settings with 3-way status badge"
      contains: "TradeBadge"
    - path: "frontend/src/pages/trading/TradingDashboardPage.tsx"
      provides: "LIVE dashboard coming-soon placeholder + circuit-breaker banner slot"
      contains: "준비 중"
    - path: "frontend/src/pages/trading/TradingHistoryPage.tsx"
      provides: "LIVE history coming-soon placeholder"
      contains: "준비 중"
  key_links:
    - from: "frontend/src/pages/trading/TossSettingsPage.tsx"
      to: "frontend/src/components/trading/ui"
      via: "TradeBadge for 3-way status"
      pattern: "components/trading/ui"
---

<objective>
Reskin the common screens (DDS Agent chat, 토스 설정) and build the future-slot placeholders (LIVE 대시보드/거래 이력 coming-soon + 서킷 브레이커 banner slot, Phase 7 TradingView slot, Phase 8 승격 게이트 slot) — all disabled, no real implementation (D5/D7). Matches wireframe sections "DDS Agent", "토스 설정", "LIVE 스텁", "Phase 7/8 슬롯".

Purpose: Close out the reskin with the remaining live screens and the explicitly non-functional placeholders so the IA is visually complete without scope creep into Phase 7/8 features.
Output: Two themed common screens + two expanded LIVE stub pages carrying the future-slot visuals.

Behavior unchanged: No backend/REST/DTO edits. DDS Agent keeps its setTimeout mockup (D5) — no real API wiring. Toss settings keeps its existing save/refresh handlers. LIVE/Phase7/8 slots are static, disabled placeholders only (D7) — no data fetching, no enabled actions. Korean strings preserved.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.8-trading-ui-redesign/06.8-RESEARCH.md
@.planning/phases/06.8-trading-ui-redesign/06.8-01-SUMMARY.md
@wireframe.html
@frontend/src/pages/trading/TradingChatPage.tsx
@frontend/src/pages/trading/TossSettingsPage.tsx
@frontend/src/pages/trading/TradingDashboardPage.tsx
@frontend/src/pages/trading/TradingHistoryPage.tsx

<interfaces>
<!-- Wave 1 primitives — see 06.8-01-SUMMARY.md -->
TradeButton, TradeBadge(variant: active|draft|running|stopped|up|down|info|warning),
TradeCard(title?), TradeInput, TradePageState.

<!-- Wireframe anchors: DDS Agent ~97–135, 토스 설정 ~175–215, LIVE 스텁 ~586–599, Phase7/8 슬롯 ~601–627. -->
<!-- Toss 3-way badge: 미설정 (draft/muted outline) / 설정됨·유효 (up: text-trade-up bg-trade-up-soft) / 설정됨·만료 (down: text-trade-down bg-trade-down-soft). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin DDS Agent chat + 토스 설정</name>
  <files>
    frontend/src/pages/trading/TradingChatPage.tsx,
    frontend/src/pages/trading/TossSettingsPage.tsx
  </files>
  <action>
TradingChatPage (DDS Agent — wireframe ~97–135; D5 reskin only, KEEP setTimeout mockup):
- Header: "DDS Agent" + status dot (trade-up "대기 중") + sub; STATE chip (bg-trade-surface text-trade-muted font-trade-mono).
- Message bubbles: assistant → bg-trade-surface, user → bg-trade-elevated, text-trade-body, font-trade-sans; timestamps font-trade-mono text-trade-muted; inline pnl numbers text-trade-up/down.
- Typing indicator dots → trade-muted.
- Input row: TradeInput (or themed textarea) bg-trade-surface placeholder text-trade-muted; 전송 → TradeButton primary, DISABLED when empty or responding (keep existing disabled logic). Auto-height + auto-scroll behavior unchanged.

TossSettingsPage (wireframe ~175–215; reskin, keep save/refresh handlers):
- Header "토스 설정" + sub.
- Status card: dot + "설정됨 · 토큰 유효" + 토큰 만료 time (font-trade-mono). 3-way status badge row → TradeBadge: 미설정=draft, 설정됨·유효=up/active, 설정됨·만료=down — driven by existing status state (swap presentation only).
- Client ID / Client Secret → TradeInput (masked, mono); 자격증명 저장 → TradeButton primary (DISABLED when ID/Secret empty — keep condition); 토큰 수동 갱신 → TradeButton secondary (DISABLED while refreshing).
- SUCCESS message text-trade-up, ERROR text-trade-down, LOADING "확인 중…" — keep exact strings.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/pages/trading/TradingChatPage.tsx frontend/src/pages/trading/TossSettingsPage.tsx
  </verify>
  <done>Build green; DDS chat themed with mockup intact; Toss settings themed with 3-way badge + save/refresh disabled conditions preserved; strings byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: LIVE stub placeholders + Phase 7/8 future slots (disabled, D7)</name>
  <files>
    frontend/src/pages/trading/TradingDashboardPage.tsx,
    frontend/src/pages/trading/TradingHistoryPage.tsx
  </files>
  <action>
Expand the two 8-line stubs into trade-themed coming-soon placeholders (wireframe ~586–627). STATIC ONLY — no data fetching, no enabled actions (D7).

TradingDashboardPage (LIVE 대시보드 stub):
- "🚧 준비 중" chip (border-trade-primary text-trade-primary) + title "LIVE 화면".
- 서킷 브레이커 경고 배너 SLOT: bg-trade-down-soft border-trade-down/30 text-trade-down — static placeholder text "서킷 브레이커 경고 배너 자리 — API 연속 실패 시 평가 중단 (Phase 8 슬롯)".
- Dashed-border placeholder box (border-trade-hairline dashed): "PAPER 대시보드/거래 이력과 동형 + 실주문 상태 · 서킷 브레이커" + "Phase 8에서 실데이터화 · 현재 준비 중 플레이스홀더".
- Phase 7 slot card: TV 배지 (text-trade-info border), bullet list (룰 목록 TV 배지 / 신호 소스 분기 / webhook URL row with disabled "복사" text-trade-primary / 사전 등록 종목 풀 / "TradingView에서 보기" 링크), "LATER · coming soon" (text-trade-primary). All non-interactive.
- Phase 8 slot card: "P8" 배지, 승격 게이트 checklist (✓ 토스 인증 완료 / ✗ 최소 5거래일 운영 (현재 3일)), "LIVE로 승격 (비활성)" → disabled TradeButton (or bg-trade-elevated text-trade-muted), "LATER" note.

TradingHistoryPage (LIVE 거래 이력 stub):
- Same "🚧 준비 중" pattern + dashed placeholder ("LIVE 거래 이력 — Phase 8에서 실데이터화"). Keep it minimal; no future-slot duplication needed beyond the coming-soon card.

Both pages remain behind ModeGuard mode=LIVE (router unchanged). No imports of data hooks.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/pages/trading/TradingDashboardPage.tsx frontend/src/pages/trading/TradingHistoryPage.tsx
    no-data-fetch check: grep -n "useQuery\|fetch\|axios\|api" frontend/src/pages/trading/TradingDashboardPage.tsx frontend/src/pages/trading/TradingHistoryPage.tsx (expect none — placeholders are static)
  </verify>
  <done>Build green; both LIVE stubs render coming-soon placeholders + circuit-breaker slot + Phase 7/8 disabled slots; no data fetching or enabled actions; all trade tokens.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` passes.
- Token guards (c-1/c-2) over all 4 files: 0 hex, 0 cream classes.
- Token-leak guard (c-3): trade-* still confined to /trading.
- Visual (browse + visual-verdict): DDS chat, Toss settings (3 badge states), LIVE dashboard/history placeholders, Phase 7/8 slots vs wireframe.
- Full phase non-regression: `npm run test:e2e` GREEN; cream routes (/app/history, /companies/:id) intact.
</verification>

<success_criteria>
SC1 (tokens), SC2 (DDS mockup + Toss handlers preserved, D5), SC7 (LIVE + Phase 7/8 as disabled coming-soon placeholders only, D7) satisfied. Phase 6.8 reskin complete across all 15 screen areas.
</success_criteria>

<output>
After completion, create `.planning/phases/06.8-trading-ui-redesign/06.8-05-SUMMARY.md`. Also record the 6.8 reskin under a new `## [v…] Trading UI 개편` entry in DESIGN.md (single-file design doc — do NOT create a new design md).
</output>
