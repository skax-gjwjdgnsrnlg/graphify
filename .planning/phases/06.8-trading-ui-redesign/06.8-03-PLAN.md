---
phase: 06.8-trading-ui-redesign
plan: 03
type: execute
wave: 3
depends_on: ["06.8-01"]
files_modified:
  - frontend/src/pages/trading/paper/PaperDashboardPage.tsx
  - frontend/src/pages/trading/paper/PaperHistoryPage.tsx
  - frontend/src/pages/trading/paper/PaperReportPage.tsx
  - frontend/src/pages/trading/TradingMonitorPage.tsx
  - frontend/src/components/backtest/CandleSection.tsx
autonomous: true
requirements: [UIX-01]
user_setup: []

must_haves:
  truths:
    - "모의 대시보드 shows 4 stat cards + positions + collapsible 실계좌 and keeps 30s polling"
    - "모의 거래 이력 reuses CandleSection (D2) with rationale accordion; first-trade auto-select preserved"
    - "모의 성과 리포트 reuses EquityCurveChart (D2) wrapped in trade surface"
    - "동작 모니터링 (PAPER data, D3) shows market status, scheduler time, signal log, today's fills with 30s polling"
    - "CandleSection internal chart canvas is unchanged; only its 3 non-canvas states + wrapper are re-themed via TradePageState"
  artifacts:
    - path: "frontend/src/pages/trading/paper/PaperDashboardPage.tsx"
      provides: "Reskinned dashboard, 4 TradeStatCard + positions + 실계좌 fold"
      contains: "TradeStatCard"
    - path: "frontend/src/pages/trading/paper/PaperHistoryPage.tsx"
      provides: "History with reused CandleSection + rationale accordion"
      contains: "CandleSection"
    - path: "frontend/src/pages/trading/TradingMonitorPage.tsx"
      provides: "PAPER monitor — signal log + fills + market/scheduler status"
      contains: "TradeBadge"
    - path: "frontend/src/components/backtest/CandleSection.tsx"
      provides: "Re-themed states (EmptyState/ErrorBanner/SkeletonBlock tone=dark → TradePageState), canvas untouched"
      contains: "TradePageState"
  key_links:
    - from: "frontend/src/pages/trading/paper/PaperHistoryPage.tsx"
      to: "frontend/src/components/backtest/CandleSection.tsx"
      via: "reused candle section (D2)"
      pattern: "CandleSection"
    - from: "frontend/src/pages/trading/paper/PaperReportPage.tsx"
      to: "frontend/src/components/backtest/EquityCurveChart.tsx"
      via: "reused equity chart (D2)"
      pattern: "EquityCurveChart"
---

<objective>
Reskin the data cluster — 모의 대시보드, 모의 거래 이력, 모의 성과 리포트, 동작 모니터링 — to the Binance dark theme, reusing the existing chart components (D2) wrapped in trade surfaces, and re-theming only CandleSection's three non-canvas states. Preserve all 30s polling, rationale accordions, first-trade auto-select, and state matrices.

Purpose: These screens drive the daily PAPER workflow and exercise the e2e regression suite (candle-chart.spec.ts). The reskin must keep CandleChart's canvas byte-identical and keep the DOM text strings the e2e relies on.
Output: Four trade-themed data screens + a re-themed CandleSection state layer matching wireframe sections "모의 대시보드", "모의 거래 이력", "모의 성과 리포트", "동작 모니터링".

Behavior unchanged: No backend/REST/DTO edits. EquityCurveChart.tsx, CandleChart.tsx, candleIndicators.ts are NOT modified (D2 — diff must be 0). React-query polling intervals, rationale-fetch logic, and the PaperHistory first-load auto-select useEffect are preserved. Korean state strings stay byte-identical (Pitfall 8 — e2e depends on them).
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.8-trading-ui-redesign/06.8-RESEARCH.md
@.planning/phases/06.8-trading-ui-redesign/06.8-01-SUMMARY.md
@wireframe.html
@frontend/src/pages/trading/paper/PaperDashboardPage.tsx
@frontend/src/pages/trading/paper/PaperHistoryPage.tsx
@frontend/src/pages/trading/paper/PaperReportPage.tsx
@frontend/src/pages/trading/TradingMonitorPage.tsx
@frontend/src/components/backtest/CandleSection.tsx
@frontend/e2e/candle-chart.spec.ts

<interfaces>
<!-- Wave 1 primitives — see 06.8-01-SUMMARY.md -->
TradeStatCard(label,value,sub?,valueColor?), TradeCard(title?), TradeBadge(variant),
TradeTable/TradeTableRow/TradeTableHeader, TradePageState(variant,title?,message?,onRetry?).

<!-- Chart reuse contract (D2 — DO NOT edit internals) -->
EquityCurveChart props: { data: BacktestEquityPoint[]; drawdownSegments: DrawdownSegment[]; initialCash: number }
CandleSection props: { symbol, date, trades, indicators, highlightTime?, highlightSide? }
  // internal states: no-selection/loading/error → swap to TradePageState; success canvas (CandleChart) untouched.
Chart wrapper swap: border-white/10 bg-gray-900/50 → border-trade-hairline bg-trade-surface.

<!-- pnl color helper pattern: value>0 text-trade-up : value<0 text-trade-down : text-trade-muted -->
<!-- Wireframe anchors: 대시보드 ~217–262, 거래 이력 ~263–317, 리포트 ~516–541, 모니터 ~543–584. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme CandleSection states + reskin 모의 대시보드</name>
  <files>
    frontend/src/components/backtest/CandleSection.tsx,
    frontend/src/pages/trading/paper/PaperDashboardPage.tsx
  </files>
  <action>
CandleSection.tsx (states only — canvas untouched): replace the three non-canvas branches that use cream shared/ with tone="dark" — no-selection EmptyState → TradePageState variant="empty"; loading SkeletonBlock → variant="loading"; error ErrorBanner → variant="error". Pass the SAME Korean message strings currently used (Pitfall 8). Swap the wrapper `rounded-lg border border-white/10 bg-gray-900/50 p-4` → `border-trade-hairline bg-trade-surface`. Rationale/marker text in the success area: text-emerald-400→text-trade-up, text-red-400→text-trade-down, text-gray-400→text-trade-muted, bg-gray-800/40→bg-trade-elevated/40. DO NOT touch the <CandleChart> element or its props.

PaperDashboardPage.tsx (wireframe ~217–262): preserve the 30s react-query polling + all data hooks.
- Header: title "모의 대시보드" + sub; right "30초 자동 갱신 · {time}" font-trade-mono text-trade-muted.
- 4-card grid (총 평가금액, 현금 잔고, 오늘 실현손익, 활성 PAPER_LIVE 룰 등 existing metrics) → TradeStatCard; values font-trade-mono; pnl uses up/down/neutral valueColor.
- Positions table → TradeTable keeping columns (종목/수량/평균단가/평가손익); 평가손익 cell text-trade-up/down via existing pnl helper (swap tokens only).
- 실계좌(Toss) section: keep collapsible/fold behavior; reskin to bg-trade-surface; empty/unconfigured → TradePageState or inline trade-muted note (keep existing strings).
- LOADING/ERROR/EMPTY → trade tokens / TradePageState, strings preserved.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/components/backtest/CandleSection.tsx frontend/src/pages/trading/paper/PaperDashboardPage.tsx
    chart-internals untouched: git diff --stat frontend/src/components/backtest/CandleChart.tsx frontend/src/components/backtest/EquityCurveChart.tsx frontend/src/components/backtest/candleIndicators.ts (must show no changes)
  </verify>
  <done>Build green. CandleSection states use TradePageState with original strings; CandleChart/EquityCurve/candleIndicators diff = 0. Dashboard shows 4 stat cards + positions + 실계좌 fold; 30s polling intact.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin 모의 거래 이력 (PaperHistoryPage) + 모의 성과 리포트 (PaperReportPage)</name>
  <files>
    frontend/src/pages/trading/paper/PaperHistoryPage.tsx,
    frontend/src/pages/trading/paper/PaperReportPage.tsx
  </files>
  <action>
PaperHistoryPage (wireframe ~263–317): preserve the first-trade auto-select useEffect (triggers on data only), 30s polling, and row-click→candle-switch + rationale-accordion toggle.
- Header + "30초 자동 갱신". CandleSection reused (from Task 1) inside a TradeCard.
- Trade table → TradeTable; columns (체결시각/종목/구분/수량/가격/손익); 구분 매수=text-trade-up / 매도=text-trade-down (match existing semantics — note wireframe shows 매도 green / 매수 red rows, but KEEP the current code's BUY/SELL color mapping to avoid behavior change); numerics font-trade-mono; 손익 up/down/neutral. Selected row → border-l-2 border-trade-primary bg-trade-elevated.
- Rationale accordion uses TradeRationaleRow (already reskinned in 06.8-02) or inline trade tokens; keep "근거 정보 없음" fallback.
- EMPTY "체결된 모의 거래가 없습니다." / ERROR "거래 이력을 불러오지 못했습니다." → TradePageState, strings byte-identical.

PaperReportPage (wireframe ~516–541): preserve data hooks.
- Header "모의 성과 리포트" + 기간 label (font-trade-mono dates).
- EquityCurveChart reused (D2) inside TradeCard (wrapper border-trade-hairline bg-trade-surface); do not touch chart props/internals.
- 6 metric cards (총 수익률, MDD, 승률, 총 거래, Sharpe, Sortino) → TradeStatCard; 수익률 up, MDD down valueColor.
- EMPTY "모의 실행 데이터가 없습니다. PAPER_LIVE 룰 활성화 시 자동 기록." → TradePageState.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test:e2e</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/pages/trading/paper/PaperHistoryPage.tsx frontend/src/pages/trading/paper/PaperReportPage.tsx
  </verify>
  <done>Build green; candle-chart.spec.ts 4 tests GREEN (auto-select + row-click + DOM order preserved). EquityCurveChart reused, no internal edits. Both pages match wireframe; strings preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin 동작 모니터링 (TradingMonitorPage — PAPER data, D3)</name>
  <files>frontend/src/pages/trading/TradingMonitorPage.tsx</files>
  <action>
Wireframe ~543–584. Preserve the 30s polling + all data hooks. This page is now reached in PAPER mode (router IA done in 06.8-01).
- Header "동작 모니터링" + "30초 자동 갱신 · {time}" font-trade-mono.
- Status row: 시장 상태 card (OPEN/CLOSED — dot trade-up when open) + 스케줄러 최근 실행 card (font-trade-mono time) → TradeCard/TradeStatCard.
- 신호 로그 table (최대 50건): columns 시각/종목/신호/체결/RSI14/SMA20/현재가. 신호 badge → TradeBadge (매수=up, 매도=down, 관망=draft/stopped). 체결 ✓ → text-trade-up, — → text-trade-muted. Indicator + price cells font-trade-mono.
- 오늘 체결 내역 table: 시각/종목/구분/수량/체결가/손익; 손익 up/down/neutral, numerics font-trade-mono.
- EMPTY: 신호 "최근 신호 없음" / 체결 "오늘 체결 내역 없음"; ERROR text-trade-down inline → keep strings (TradePageState or inline). LOADING "불러오는 중…".
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/pages/trading/TradingMonitorPage.tsx
  </verify>
  <done>Build green; monitor page themed, 30s polling intact, signal/fill tables match wireframe, strings preserved. Reachable in PAPER mode.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build && npm run test:e2e` — build passes, candle-chart.spec.ts GREEN (HIGH-risk regression gate).
- Token guards (c-1/c-2) over all 5 files: 0 hex, 0 cream classes.
- D2 guard: `git diff --stat` on CandleChart.tsx, EquityCurveChart.tsx, candleIndicators.ts → no changes.
- Visual (browse + visual-verdict): all 4 states for each of the 4 screens vs wireframe; charts sit on trade-surface; numbers in JetBrains Mono.
</verification>

<success_criteria>
SC1 (tokens), SC2 (polling + rationale + auto-select preserved), SC3 (chart reuse on dark surface), SC4 (empty/loading/error/success states), SC6 (monitor in PAPER) satisfied for the data cluster.
</success_criteria>

<output>
After completion, create `.planning/phases/06.8-trading-ui-redesign/06.8-03-SUMMARY.md` noting the CandleSection state-swap pattern (so 06.8-04 backtest reuses it identically).
</output>
